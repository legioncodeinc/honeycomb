# QA Findings Report — PRD-060 ROI Tracker

> **Audited:** 2026-06-26 · **Branch:** `legion/sharp-wilson-029812` · **Auditor:** quality-worker-bee (final close-out)
> **Source plan:** `library/requirements/backlog/prd-060-roi-tracker/` (index + 060a..060f)
> **Authoritative AC list + per-wave evidence:** `library/ledger/EXECUTION_LEDGER-prd-060.md`
> **Ordering:** `security-worker-bee` ran first and returned zero Critical/High (clean). This is the first report in `reports/` (only `.gitkeep` present) — quality ran last, in order. No ordering violation.

---

## Summary

PRD-060 (ROI Tracker) is **VERIFIED**. All 55 sub-ACs across the six sub-PRDs (060a–060f) and all 14 module ACs are implemented in code AND proven by passing tests that I read directly (not taken on the ledger's word). The measured-vs-modeled-vs-allocated honesty contract is structurally enforced by the type system (a compile-time `@ts-expect-error` witness in non-test code that the `tsc` gate enforces), integer cents hold end-to-end with float-cents rejected at the wire boundary, the per-user gate is exactly `verifiedClaim?.source === 'backend-token' ? userId : ''` with no spoofable fallback, and every degraded path fails soft to a dash-glyph rather than a fabricated `$0.00`. There is **no PRD-061 scope bleed**. My own verification commands all passed: `npm run typecheck` (exit 0), `npm run audit:sql` (exit 0, 240 files, every interpolation guarded), and the affected vitest suites (104 files, **986 passed / 1 skipped / 0 failed**).

Two items are **CONCERN (non-blocking, by design + operator decision)**: the modeled-assumption string is a clearly-labeled placeholder pending operator sign-off (`signedOff: false`, surfaced honestly in the UI as "provisional — not yet signed off"), and the per-user **live-populated** rollup is BLOCKED on a backend `backend-token` claim that does not exist yet — the GATE behavior is fully implemented and tested, only the live population is parked. Both are documented dependencies, not defects.

**Overall verdict: PASS (ship-ready for PRD-060 scope). Zero Critical. Zero Warning. Two documented CONCERN/BLOCKED items carried forward as known dependencies.**

---

## Scorecard

| Axis | Status | Notes |
|---|---|---|
| **Completeness** | ✅ PASS | All 55 sub-ACs + 14 module ACs implemented. The two parked items (assumption sign-off, live per-user) are by-design dependencies with their mechanisms fully built. |
| **Correctness** | ✅ PASS | Honesty contract is type-enforced (compile-time witness in non-test code). Integer cents rounded at the arithmetic edge; float-cents `.catch()`-rejected on the wire. Net never computed from incomplete inputs. |
| **Alignment** | ✅ PASS | Code matches the PRD intent precisely: one registry entry + one page (no sidebar/router hand-edit), tenant-scoped BIGINT ledger, append-only + version-bumped patterns, `read_policy`-scoped read, daemon-sole-egress. |
| **Gaps** | ✅ PASS | No silent gaps. Every absent/partial/unreachable/unauthenticated state has an explicit discriminant and a dash-glyph render. `cost_basis:'none'` at write time is the honest choice (no fabricated per-session infra split). |
| **Detrimental patterns** | ✅ PASS | No spoofable identity fallback, no float money, no second billing egress, no fabricated `$0`, no scope bleed into PRD-061, no raw SQL interpolation. |

---

## Verification commands (run by me, this audit)

| Command | Result |
|---|---|
| `npm run typecheck` (`tsc --noEmit`) | **exit 0 — clean.** (This gate enforces the b-AC-4 honesty-contract witness, which lives in non-test code.) |
| `npm run audit:sql` | **exit 0 — "every SQL interpolation routes through an escaping helper"**, 240 files scanned under `src/daemon`, `src/daemon-client`. |
| `npx vitest run` over `tests/daemon/runtime/{dashboard,capture,inference}`, `tests/daemon/storage/catalog`, `tests/dashboard`, `tests/hooks` | **104 files, 986 passed, 1 skipped, 0 failed.** Includes `roi-page.test.tsx` (32), `roi-skillify-meter` (7), and the heavy `hook-runtime.test.ts` (23, all green). |
| PRD-046d session-prime flake check | The flake lives in `tests/hooks/runtime/hook-runtime.test.ts` (a PRD-046 file — touches **no** PRD-060 file). It **passed cleanly here** (all 7 PRD-046d session-prime tests green). Confirmed a CPU-saturation per-test timeout under heavy parallel load, **unrelated to PRD-060**. The ledger's `npx vitest run --no-file-parallelism → 3501 passed / 0 failed` baseline corroborates. |

---

## Critical Issues (must fix)

**None.**

---

## Warnings (should fix)

**None.**

---

## CONCERN / BLOCKED (documented dependencies, non-blocking for PRD-060 scope)

### CONCERN-1 — Modeled-assumption string is a placeholder pending operator sign-off (by design)
The memory-injection assumption constant is honestly flagged `signedOff: false` and the UI surfaces "provisional — not yet signed off". The *mechanism* (assumption-as-data, single source feeding both the ⓘ popover and the page-foot footnote) is fully built per b-AC-3 / e-AC-8.
- `src/daemon/runtime/dashboard/roi-savings.ts:238-248` — `MEMORY_INJECTION_ASSUMPTION` with `signedOff: false` and a `TODO(roi-assumption-signoff)` marker at `:229-233`.
- `src/dashboard/web/pages/roi.tsx:220-224` — renders the "provisional — not yet signed off" caption when `!assumption.signedOff`.
- **Action:** operator signs off the exact model + constants + disclosure string; edit the one constant and flip `signedOff` to `true`. No code change beyond that constant. This is the gating open question in PRD-060b, parked as a DECISION, not a code blocker.

### BLOCKED-1 — Live per-user rollup awaits the verified `backend-token` claim (per operator)
The per-user **gate** is fully implemented and tested (f-AC-6/7, e-AC-14, AC-12 gate path). The live **population** is parked because no backend issues a verified person claim today.
- `src/daemon/runtime/dashboard/roi-ledger.ts:90-95` — `resolveGatedUserId` returns `claim.userId` only when `source === 'backend-token'`, `''` otherwise, with **no** git-email/`$USER`/OS-login branch by construction.
- `src/daemon/runtime/dashboard/api.ts:931` — `perUserAvailable: false` hardcoded (no verified claim exists).
- `src/dashboard/web/pages/roi.tsx:577-581` — the "Per-user requires verified login" empty state renders when the flag is false; never a `$0` or self-asserted name.
- **Action:** when the backend claim lands, thread a `verifiedClaim` into the session writer and flip `perUserAvailable`. The gate behavior is DONE; only live population is BLOCKED, exactly as the operator scoped it.

---

## Suggestions (consider improving)

### SUG-1 — f-AC-12 "lookup index" assertion is structural-by-construction, not an explicit positive test
The absence of BM25/vector indexes on `roi_metrics`/`teams` is *guaranteed* (both declare `embeddingColumns: []`, so no vector index is built; no `deeplake_index` is declared). The catalog test (`tests/daemon/storage/catalog/catalog.test.ts:71-79`) asserts both tables are registered tenant tables, and the load-time guard enforces the DEFAULT-on-NOT-NULL invariant (f-AC-10). However, there is no *explicit* positive test asserting "lookup indexes exist on `org_id`/`workspace_id`/`team_id`/`period_start`". Indexing on this backend is lookup-derived rather than declared per-column, so this is informational only — consider a one-line inspection test documenting the no-BM25/no-vector intent for future readers. Not a defect.

---

## Plan Item Traceability

Legend: **PASS** = implemented + proven by a test I read · **CONCERN** = implemented, carries a documented caveat · **BLOCKED** = gate done, live path parked per operator.

### 060a — Token & Cache Usage Capture

| AC | Implementing file(s) | Proving test(s) | Verdict |
|---|---|---|---|
| a-AC-1 (optional `usage`, absent≠zero) | `event-contract.ts:61-107` (`TurnUsageSchema` `.optional()`, all fields optional) | `tests/daemon/runtime/capture/capture-token-usage.test.ts` | PASS |
| a-AC-2 (shim extracts 4 counts) | `hooks/claude-code/shim.ts:89-93`, `hooks/normalize.ts:265-280` | `tests/hooks/claude-code/usage-extraction.test.ts` | PASS |
| a-AC-3 (5 additive cols, idempotent heal) | `sessions-summaries.ts` (token + `source_tool` cols), `catalog/index.ts` | `tests/daemon/storage/catalog/sessions-usage-columns.test.ts` | PASS |
| a-AC-4 (missing cols → absent, daemon boots) | additive-heal path; nullable cols | `sessions-usage-columns.test.ts`, `catalog.test.ts` | PASS |
| a-AC-5 (same append-only INSERT) | `capture-handler.ts` (counts on the turn INSERT) | `capture-token-usage.test.ts` | PASS |
| a-AC-6 (malformed → null, never silent 0) | `normalize.ts:305-309` (`readCount` rejects non-int/negative → `undefined`) | `usage-extraction.test.ts` | PASS |
| a-AC-7 (`source_tool="claude-code"`) | `shim.ts`, `sessions-summaries.ts` | `capture-token-usage.test.ts` | PASS |

### 060b — Cost & Savings Engine + Rate Table

| AC | Implementing file(s) | Proving test(s) | Verdict |
|---|---|---|---|
| b-AC-1 (rate table, cache 0.1×/1.25×, "rates as of") | `roi-rates.ts:28-101` (`anthropicCacheRate`, `RATE_TABLE`, `RATES_AS_OF`) | `tests/daemon/runtime/dashboard/roi-rates.test.ts` | PASS |
| b-AC-2 (measured savings, tagged `measured`) | `roi-savings.ts:139-157` (`measuredCacheSavings` → `Measured<…>`) | `roi-savings.test.ts` | PASS |
| b-AC-3 (modeled, assumption-as-data) | `roi-savings.ts:214-283` (`MemoryInjectionAssumption`, `modeledMemoryInjectionSavings` → `Modeled<…>`) | `roi-savings.test.ts` | PASS (assumption=placeholder, see CONCERN-1) |
| b-AC-4 (no measured-from-modeled; structural) | `roi-savings.ts:52-77` (`Measured`/`Modeled` wrappers), `roi-honesty-contract.ts:31-57` (`@ts-expect-error` witness enforced by `tsc`) | typecheck gate (exit 0) + `roi-savings.test.ts` | PASS |
| b-AC-5 (`blendedCentsPerMtok` null when absent) | `roi-savings.ts:172-199` (returns `null` when `totalTokens===0`) | `roi-savings.test.ts` | PASS |
| b-AC-6 (integer cents, no float crosses boundary) | `roi-savings.ts:84-91` (`isIntegerCents`, `tokensAtRate` rounds) | `roi-savings.test.ts` | PASS |
| b-AC-7 (absent/partial, not 0-as-measured) | `roi-savings.ts:98-156` (`CaptureStatus`, NULL skipped not zeroed) | `roi-savings.test.ts` | PASS |

### 060c — DeepLake Billing Integration

| AC | Implementing file(s) | Proving test(s) | Verdict |
|---|---|---|---|
| c-AC-1 (summary + usage/compute, injected fetch) | `roi-billing.ts` (creds-gated client) | `tests/daemon/runtime/dashboard/roi-billing.test.ts` | PASS |
| c-AC-2 (no creds→unauthenticated; unreachable→unreachable; no fabricated value) | `roi-billing.ts:395` (creds gate), status union | `roi-billing.test.ts` | PASS |
| c-AC-3 (retry 429/5xx, bounded timeout, bearer redacted) | `roi-billing.ts:244,291-294` (token in Authorization header only; truncated error body) | `roi-billing.test.ts` (token-never-in-logs assertion) | PASS |
| c-AC-4 (TTL in-memory cache) | `roi-billing.ts` (`createInfraCostReadModel` TTL) | `roi-billing.test.ts` | PASS |
| c-AC-5 (`session_type` breakdown, itemized+summable) | `roi-billing.ts` (`sessionTypes`, `sessionTypeTotalCents`) | `roi-billing.test.ts` | PASS |
| c-AC-6 (integer cents) | `roi-billing.ts` | `roi-billing.test.ts` | PASS |
| c-AC-7 (partial status, missing flagged not zeroed) | `roi-billing.ts` (`partial` status) | `roi-billing.test.ts` | PASS |

### 060d — Pollination Cost Metering

| AC | Implementing file(s) | Proving test(s) | Verdict |
|---|---|---|---|
| d-AC-1 (transport surfaces discarded `usage`) | `transport-anthropic.ts:42-132` (`UsageSink`/`UsageReport`, no-op default, success-path only) | `tests/daemon/runtime/inference/transport-anthropic-usage.test.ts` | PASS |
| d-AC-2 (Haiku priced via 060b, integer cents) | `roi-pollination.ts:179-187` (`priceHaikuTokens`) | `tests/daemon/runtime/dashboard/roi-pollination.test.ts` | PASS |
| d-AC-3 (DeepLake half, no 2nd billing read) | `roi-pollination.ts:244-255` (takes snapshot, no `read()`) | `roi-pollination.test.ts` (zero-fetch assertion) | PASS |
| d-AC-4 (`pollination = haiku + deeplake`, itemized) | `roi-pollination.ts:309-318` (`composePollinationCost`) | `roi-pollination.test.ts` | PASS |
| d-AC-5 (absent≠0, unreachable, worst-status) | `roi-pollination.ts:267-290` (`STATUS_SEVERITY`, `worstPollinationStatus`) | `roi-pollination.test.ts` | PASS |
| d-AC-6 (integer cents) | `roi-pollination.ts:165-167` (`tokensAtRate` rounds) | `roi-pollination.test.ts` | PASS |

### 060f — Shared Spend Ledger + Teams Roster

| AC | Implementing file(s) | Proving test(s) | Verdict |
|---|---|---|---|
| f-AC-1 (`roi_metrics` tenant-scoped, queryable org/workspace) | `tenancy.ts:256-283,376-381` (`scope:"tenant"`) | `tests/daemon/storage/catalog/catalog.test.ts:71-79`, `roi-ledger.test.ts` | PASS |
| f-AC-2 (append-only; re-price appends, no UPDATE) | `roi-ledger.ts:212-252` (`appendOnlyInsert`, no mutate path), `tenancy.ts:376-381` | `tests/daemon/runtime/dashboard/roi-ledger.test.ts` | PASS |
| f-AC-3 (canonical = `MAX(created_at)`, original retained) | `roi-ledger.ts:381-409` (NOT EXISTS self-join) | `roi-ledger.test.ts` | PASS |
| f-AC-4 (all money BIGINT cents, no float) | `tenancy.ts:271-275` (BIGINT cols), `roi-ledger.ts:193-196` (`toInt`) | `roi-ledger.test.ts` | PASS |
| f-AC-5 (measured/modeled/allocated separate; mixed-basis detectable) | `tenancy.ts:271-278` (`cost_basis`, `allocation_method`), `api.ts:744-782` (`computeRollups` `mixedBasis`) | `roi-ledger.test.ts`, `api-roi.test.ts` | PASS |
| f-AC-6 (`user_id` only from `backend-token`, no fallback) | `roi-ledger.ts:90-95` (`resolveGatedUserId`) | `roi-ledger.test.ts` | PASS / BLOCKED-1 (live) |
| f-AC-7 (no historical backfill) | `roi-ledger.ts` (append-only; pre-claim rows keep `''`) | `roi-ledger.test.ts` | PASS |
| f-AC-8 (`teams` version-bumped, agent/user union) | `tenancy.ts:305-319,382-390`, `roi-ledger.ts:285-308` (`upsertTeamMember`) | `roi-ledger.test.ts` | PASS |
| f-AC-9 (`team_id` resolved at write, fail-soft) | `roi-ledger.ts:111-137` (`resolveTeamId`, catch→`''`) | `roi-ledger.test.ts` | PASS |
| f-AC-10 (additive-heal, every NOT NULL has DEFAULT; degrade-not-throw) | `tenancy.ts` (all DEFAULTs), `roi-ledger.ts:381-409` (`shared-ledger-absent`) | `catalog.test.ts:86-96` (load-time guard), `roi-ledger.test.ts` | PASS |
| f-AC-11 (SQL-guarded writes) | `roi-ledger.ts` (`val.*`, `sLiteral`, `sqlIdent` throughout) | `audit:sql` (exit 0), `roi-ledger.test.ts` | PASS |
| f-AC-12 (lookup indexes only, no BM25/vector) | `tenancy.ts:379,389` (`embeddingColumns: []`) | `catalog.test.ts` (registration) | PASS (see SUG-1) |
| f-AC-13 (read scoped via `read_policy`) | `roi-ledger.ts:346-368` (`buildRoiReadScopeSql`, fail-closed) | `roi-ledger.test.ts` | PASS |

### 060e — ROI Tracker Dashboard Page

| AC | Implementing file(s) | Proving test(s) | Verdict |
|---|---|---|---|
| e-AC-1 (one registry entry, no sidebar/router edit, PageFrame "ROI") | `registry.tsx:213` (one entry), `roi.tsx:711` | `tests/dashboard/web/registry.test.tsx`, `roi-page.test.tsx`; git: only registry.tsx + tests touched, no sidebar/router source | PASS |
| e-AC-2 (pure function of `RoiView`, all statuses) | `roi.tsx:658-755`, `api.ts:844-937` (`assembleRoiView` pure) | `tests/dashboard/web/roi-page.test.tsx`, `api-roi.test.ts` | PASS |
| e-AC-3 (four signals; modeled `est.`; net inherits) | `roi.tsx:114-141,287,332` (tone/weight/`~`+`est.`) | `roi-page.test.tsx` | PASS |
| e-AC-4 (honey never encodes sign) | `roi.tsx:131-133` (`netSignColor`), `:329` | `roi-page.test.tsx` | PASS |
| e-AC-5 (dash glyph not `$0.00`; absent; Claude-Code-only) | `roi.tsx:69,287,359-364` (`DASH`, `PartialBadge`) | `roi-page.test.tsx` | PASS |
| e-AC-6 (billing-unreachable → dash + retry; net not from incomplete) | `api.ts:890-917` (`netComputable`), `roi.tsx:312-352` (`NetHero`) | `roi-page.test.tsx`, `api-roi.test.ts` | PASS |
| e-AC-7 (not-auth gates, redacted only, no creds to page) | `roi.tsx:624-642,706` (`AuthGate`), no `createWireClient` | `roi-page.test.tsx` | PASS |
| e-AC-8 (ⓘ + footnote from one assumption field) | `roi.tsx:168-229,741-750` (both read `assumption.assumptionText`) | `roi-page.test.tsx` | PASS |
| e-AC-9 (cost-rising-not-green, inverted delta) | `roi.tsx:148-152,419-450` (`costDeltaColor`, `CostKpi`) | `roi-page.test.tsx` | PASS |
| e-AC-10 (inline-SVG trend, dashed/solid, motion tokens) | `roi-chart.tsx`, `roi.tsx:737`; `api.ts:947-959` (honest-empty trend) | `roi-page.test.tsx` | PASS |
| e-AC-11 (integer cents on wire; dollars at render edge; null blended) | `wire.ts:245,272` (`roiCentsField = z.number().int().catch(0)`, blended `.int().nullable().catch(null)`), `roi.tsx:82-103` | `roi-page.test.tsx` | PASS |
| e-AC-12 (read at org/workspace via `read_policy`; across-device) | `api.ts:802-829` (`readRoiMetrics` scoped), `:934` (`scopedAcrossDevices`) | `api-roi.test.ts`, `roi-ledger.test.ts` | PASS |
| e-AC-13 (org/team/agent/project rollups in daemon; component no grouping) | `api.ts:724-782` (`computeRollups`), `roi.tsx:529-613` (renders only) | `api-roi.test.ts`, `roi-page.test.tsx` | PASS |
| e-AC-14 (per-user only when flag true; else empty state) | `api.ts:931` (`perUserAvailable:false`), `roi.tsx:577-581` | `roi-page.test.tsx` | PASS / BLOCKED-1 (live) |
| e-AC-15 (allocated `est.`-class, distinct; mixed-basis flagged) | `roi.tsx:139-141,533-537,549-570` (`basisIsModeled`, mixed-basis caption) | `roi-page.test.tsx` | PASS |

### Module-level ACs

| AC | Backed by | Verdict |
|---|---|---|
| AC-1 | e-AC-1 | PASS |
| AC-2 | a-AC-* + b-AC-2 | PASS |
| AC-3 (honesty: measured/modeled separated, net inherits `est.`) | b-AC-3/4 + e-AC-3 | PASS |
| AC-4 (billing fail-soft → dash) | c-AC-2 + e-AC-6 | PASS |
| AC-5 (pollination metering) | d-AC-1..4 | PASS |
| AC-6 (measured $0 ≠ unknown) | e-AC-2/5 | PASS |
| AC-7 (Claude-Code-only partial honest) | a-AC-7 + b-AC-5/7 + e-AC-5 | PASS |
| AC-8 (auth-gated, daemon sole egress) | c-AC-* + e-AC-7 | PASS |
| AC-9 (additive heal, daemon boots) | a-AC-3/4 | PASS |
| AC-10 (integer cents end-to-end) | b-AC-6 + c-AC-6 + d-AC-6 + f-AC-4 + e-AC-11 | PASS |
| AC-11 (shared ledger, cross-device) | f-AC-1/2 + e-AC-12 | PASS |
| AC-12 (per-user gate) | f-AC-6/7 + e-AC-14 | PASS (gate) / BLOCKED-1 (live population) |
| AC-13 (team/agent rollups) | f-AC-8/9 + e-AC-13 | PASS |
| AC-14 (allocated ≠ measured) | f-AC-5 + e-AC-15 | PASS |

---

## Honesty-contract spot-checks (the operator's named concerns)

- **measured/modeled/allocated structurally separated; modeled never renders/sums as measured** — VERIFIED. `Measured<T>`/`Modeled<T>` are distinct tagged wrappers (`roi-savings.ts:52-77`); `measuredCacheSavings` takes raw counts (no `Modeled` input is type-assignable); `netRoi` folds a `Modeled` term so its return is `Modeled<NetRoi>` — the `est.` taint propagates through the type. The `@ts-expect-error` witness in `roi-honesty-contract.ts` (non-test code) fails `tsc` if the types ever loosen. Typecheck passed (exit 0), so the contract holds.
- **integer cents end-to-end** — VERIFIED. Rounding only at the per-Mtok arithmetic edge (`tokensAtRate`); `z.number().int().catch(0)` rejects float-cents at the wire; dollars formed only at `roi.tsx:82`.
- **net inherits `est.`** — VERIFIED. `api.ts:905` sets `modeled:true`; `roi.tsx:316,332,335` render the `est.` badge + eyebrow.
- **per-user gate** — VERIFIED. `resolveGatedUserId` (`roi-ledger.ts:90-95`) admits only `source==='backend-token'`; `perUserAvailable:false`; empty-state in the page; no git-email/`$USER`/OS branch exists in the code.
- **fail-soft everywhere** — VERIFIED. Missing token cols → null/absent (not throw); unreachable/unauthenticated billing → status discriminant (no fabricated `$0`); missing ledger table → `shared-ledger-absent` (daemon boots); page renders `—` not `$0.00`.
- **no PRD-061 scope bleed** — VERIFIED. No PRD-061 code exists on the branch; all new modules are `roi-*.ts` (060a–060f scope) plus capture/dashboard touchpoints. The hosted cross-org surface is correctly absent.

---

## Files Changed (PRD-060 surface)

**New runtime modules (daemon):** `roi-rates.ts`, `roi-savings.ts`, `roi-honesty-contract.ts`, `roi-billing.ts`, `roi-ledger.ts`, `roi-session-writer.ts`, `roi-skillify-meter.ts`, `roi-pollination.ts` (all under `src/daemon/runtime/dashboard/`).
**New page (dashboard):** `src/dashboard/web/pages/roi.tsx`, `src/dashboard/web/pages/roi-chart.tsx`.
**Modified (additive seams):** `capture/capture-handler.ts`, `capture/event-contract.ts` (optional `usage`); `dashboard/api.ts` (composite `/roi` + `/roi/trend` read-model); `inference/index.ts`, `inference/model-client-factory.ts`, `inference/transport-anthropic.ts` (usage-surfacing seam); `skillify/worker.ts` (writer call-site); `storage/catalog/index.ts`, `catalog/sessions-summaries.ts`, `catalog/tenancy.ts` (`roi_metrics` + `teams` + token cols); `dashboard/contracts.ts`, `web/registry.tsx`, `web/wire.ts` (one ROI registry entry + wire schemas); `hooks/claude-code/shim.ts`, `hooks/index.ts`, `hooks/normalize.ts` (usage extraction).
**New tests (13):** capture-token-usage, api-roi, roi-billing, roi-ledger, roi-pollination, roi-rates, roi-savings, roi-session-writer, roi-skillify-meter, transport-anthropic-usage, sessions-usage-columns, roi-page, usage-extraction.
**Modified tests:** catalog.test.ts, tenancy.test.ts, app/registry/sidebar dashboard tests (ROI entry derives from the registry — expected, not a sidebar source edit).

---

*Report written by quality-worker-bee. Evidence over opinion: every verdict above traces to a `file:line` I read and a test I confirmed green. Report, don't fix — the two CONCERN/BLOCKED items are documented dependencies for the operator/backend, not code defects.*
