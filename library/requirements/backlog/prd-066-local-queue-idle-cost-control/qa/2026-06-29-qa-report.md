# QA Report: PRD-066 Local Queue Idle-Cost Control

**Plan document:** `library/requirements/backlog/prd-066-local-queue-idle-cost-control/`
**Audit date:** 2026-06-29
**Base branch:** `main`
**Head:** `legion/forked-database-route-reduce-cost` (`522b85f`)
**Auditor:** main thread fallback after `quality-worker-bee` could not be dispatched

## Summary

The PRD-066 implementation is partially complete: the durable local queue, hybrid routing, feature flag rollback, migration-drain behavior, local diagnostics, live idle-meter proof, and live active memory write proof are implemented and verified. The PRD is not fully verified because recall-read categorization and dogfood scenarios still require additional rollout checks.

## Scorecard

| Category | Status | Notes |
|---|---|---|
| Completeness | Partial | PRD-066a, the idle-cost criteria, and PRD-066b handler-level active writes are implemented; recall categorization and dogfood criteria remain blocked. |
| Correctness | Partial | Focused queue/routing behavior passes, live idle coordination reads are zero, and live active DeepLake writes occur without poll reads/writes. |
| Alignment | Pass | The implementation keeps DeepLake as the memory substrate and uses the local queue only as a per-device scheduler. |
| Gaps | Partial | Operator-facing diagnostics are available in the queue service but not yet exposed through CLI or dashboard. |
| Detrimental | Pass | No Critical/High security issues found in the implemented scope. |

## Critical Issues (must fix)

- [ ] **Recall-read categorization is not yet captured**, `library/requirements/backlog/prd-066-local-queue-idle-cost-control/prd-066c-local-queue-idle-cost-control-idle-cost-verification-and-rollout.md:47`

  The live integration now proves active memory writes: a real local `memory_extraction` job reaches controlled-write and graph-persist, with active pipeline poll reads=0/writes=0 and total DeepLake reads=67/writes=15. It does not yet call the recall path and classify recall reads separately from coordination polling. Suggested: extend the live proof or add a companion recall integration assertion against the written throwaway memory.

  ```md
  - AC-3: Active memory writes and recall reads are still visible and correctly categorized.
  ```

## Warnings (should fix)

- [ ] **Operator diagnostics are not yet surfaced outside the service seam**, `library/requirements/backlog/prd-066-local-queue-idle-cost-control/prd-066-local-queue-idle-cost-control-index.md:96`

  `LocalJobQueueService.counts()` can identify queued, retrying, leased, done, and failed jobs, but the QA pass did not find a CLI or dashboard surface for an operator to view those counts. Suggested: decide whether diagnostics are CLI-only, dashboard-visible, or both, then expose `counts()` accordingly.

  ```md
  9. The daemon must expose enough diagnostics to prove whether the local queue is empty, blocked, or
  ```

- [ ] **Dogfood scenarios are blocked on a real environment**, `library/requirements/backlog/prd-066-local-queue-idle-cost-control/prd-066c-local-queue-idle-cost-control-idle-cost-verification-and-rollout.md:50`

  The current test suite covers restart persistence and lease recovery at the service level, but not a long-running daemon dogfood window with sleep/wake and transient DeepLake outage scenarios. Suggested: run this as a rollout checklist after credentials and metering are available.

  ```md
  - AC-6: Dogfood rollout runs long enough to include daemon restart, sleep/wake, and transient
  ```

- [ ] **Full `npm run ci` still has a suite-load timeout unrelated to PRD-066**, `tests/property/json-parsers.property.test.ts:104`

  The full gate failed once the broad suite reached `json-parsers.property.test.ts`, timing out at 5000 ms under parallel load. The same file passed when rerun directly, so this is not evidence of a PRD-066 functional failure, but it remains a gate health issue before merge.

  ```ts
  it("a manifest entry with a traversal dirName survives only as a STRING, never executed here
  ```

## Suggestions (consider improving)

- [ ] **Expose the local queue path in diagnostics**, `src/daemon/runtime/services/local-job-queue.ts:20`

  The queue path is intentionally `.daemon/local-queue.db`. Exposing the resolved path in diagnostics would make support easier when confirming that a user is on the local queue path rather than the shared Deeplake-backed queue.

## Plan Item Traceability

| # | Plan Requirement | Status | Implementation Location | Notes |
|---|---|---|---|---|
| 066-AC-1 | Zero DeepLake coordination reads during idle window | Pass | `src/daemon/runtime/services/hybrid-job-queue.ts:112`; `tests/integration/local-queue-idle-meter-live.itest.ts:43` | Live report: shared poll reads=39, local poll reads=0/writes=0. |
| 066-AC-2 | Queued local job survives daemon restart and executes once | Pass | `src/daemon/runtime/services/local-job-queue.ts:263`; `tests/daemon/runtime/services/local-job-queue.test.ts:48` | Focused test reopens the same `.daemon/local-queue.db`. |
| 066-AC-3 | Expired local lease is reclaimed and retried without duplicate success | Pass | `src/daemon/runtime/services/local-job-queue.ts:358`; `tests/daemon/runtime/services/local-job-queue.test.ts:101` | Covered by injected-clock reclaim test. |
| 066-AC-4 | Local-only producers avoid DeepLake enqueue APIs when enabled | Pass | `src/daemon/runtime/services/hybrid-job-queue.ts:70`; `tests/daemon/runtime/services/hybrid-job-queue.test.ts:62` | Shared enqueue recorder remains empty. |
| 066-AC-5 | Local-only workers avoid DeepLake job discovery when enabled | Pass | `src/daemon/runtime/services/hybrid-job-queue.ts:79`; `tests/daemon/runtime/services/hybrid-job-queue.test.ts:75` | Also covers background reaper start at `tests/daemon/runtime/services/hybrid-job-queue.test.ts:85`. |
| 066-AC-6 | Feature flag off preserves current behavior | Pass | `src/daemon/runtime/services/hybrid-job-queue.ts:54`; `tests/daemon/runtime/services/hybrid-job-queue.test.ts:136` | Returns shared queue when disabled. |
| 066-AC-7 | Active memory write/recall still reaches DeepLake for real work | Blocked | `tests/integration/local-queue-idle-meter-live.itest.ts:101` | Active write/graph path verified; recall assertion remains. |
| 066-AC-8 | Rollout report includes before/after DeepLake coordination read counts | Pass | `library/requirements/backlog/prd-066-local-queue-idle-cost-control/qa/2026-06-29-idle-meter-live-report.md` | Shared poll reads=39, local poll reads=0/writes=0. |
| 066a-AC-1..8 | Local queue persistence, lease, retry, prune, validation, diagnostics | Pass | `src/daemon/runtime/services/local-job-queue.ts:202`; `tests/daemon/runtime/services/local-job-queue.test.ts:47` | Focused local queue suite passes. |
| 066b-AC-1..2 | Producer and worker routing avoids shared queue in local-only mode | Pass | `src/daemon/runtime/services/hybrid-job-queue.ts:70`; `tests/daemon/runtime/services/hybrid-job-queue.test.ts:62` | Includes no shared start/reaper coverage. |
| 066b-AC-3 | Handler-level DeepLake reads/writes still occur | Pass | `tests/integration/local-queue-idle-meter-live.itest.ts:101` | Live active proof: total DeepLake reads=67/writes=15 with poll reads=0/writes=0. |
| 066b-AC-4..7 | Flag off, migration drain, duplicate avoidance, unknown shared path | Pass | `src/daemon/runtime/services/hybrid-job-queue.ts:90`; `tests/daemon/runtime/services/hybrid-job-queue.test.ts:136` | Focused hybrid suite passes. |
| 066c-AC-1..2 | Baseline/post-change idle reports | Pass | `library/requirements/backlog/prd-066-local-queue-idle-cost-control/qa/2026-06-29-idle-meter-live-report.md` | Live meter report added. |
| 066c-AC-3 | Active memory reads/writes categorized separately | Blocked | `library/requirements/backlog/prd-066-local-queue-idle-cost-control/prd-066c-local-queue-idle-cost-control-idle-cost-verification-and-rollout.md:47` | Active writes categorized; recall reads still need assertion. |
| 066c-AC-4 | Rollback flag restores previous behavior | Pass | `src/daemon/runtime/services/hybrid-job-queue.ts:54`; `tests/daemon/runtime/services/hybrid-job-queue.test.ts:136` | No data migration required. |
| 066c-AC-5 | Queue diagnostics identify status counts | Partial | `src/daemon/runtime/services/local-job-queue.ts:523` | Service exists; no operator-facing surface yet. |
| 066c-AC-6 | Dogfood restart, sleep/wake, transient outage | Blocked | `library/requirements/backlog/prd-066-local-queue-idle-cost-control/prd-066c-local-queue-idle-cost-control-idle-cost-verification-and-rollout.md:50` | Needs real dogfood window. |
| 066c-AC-7 | Release notes describe boundary and remaining DeepLake cost paths | Pass | `library/requirements/backlog/prd-066-local-queue-idle-cost-control/qa/2026-06-29-release-notes-draft.md` | Draft added in PRD QA folder. |

## Files Changed

- `library/ledger/EXECUTION_LEDGER-prd-066.md` (A), AC ledger and Smoker event log for PRD-066.
- `library/requirements/backlog/prd-066-local-queue-idle-cost-control/qa/2026-06-29-idle-meter-live-report.md` (A), live before/after idle query-meter report.
- `library/requirements/backlog/prd-066-local-queue-idle-cost-control/qa/2026-06-29-qa-report.md` (A), this QA report.
- `library/requirements/backlog/prd-066-local-queue-idle-cost-control/qa/2026-06-29-security-review.md` (A), main-thread security review after worker dispatch failure.
- `library/requirements/backlog/prd-066-local-queue-idle-cost-control/qa/2026-06-29-release-notes-draft.md` (A), release-note draft for PRD-066c AC-7.
- `src/daemon/runtime/assemble.ts` (M), wires shared queue through the hybrid router and opens the local queue only when enabled.
- `src/daemon/runtime/services/hybrid-job-queue.ts` (A), routes local-only job kinds to the local queue behind flags and keeps shared unknown/migration behavior.
- `src/daemon/runtime/services/local-job-queue.ts` (A), SQLite-backed local durable queue with validation, lease, retry, prune, and counts.
- `tests/daemon/runtime/services/hybrid-job-queue.test.ts` (A), focused routing, migration, flag, and shared-reaper tests.
- `tests/daemon/runtime/services/local-job-queue.test.ts` (A), focused local queue persistence, concurrency, retry, validation, and diagnostics tests.
