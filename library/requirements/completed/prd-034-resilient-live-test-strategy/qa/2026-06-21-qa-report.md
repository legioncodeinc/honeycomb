# QA Report — PRD-034 Resilient Live-Test Strategy

**Auditor:** quality-worker-bee (quality-stinger) · **Date:** 2026-06-21 · **Branch:** `prd-034-resilient-live-test-strategy`
**Ordering:** security-stinger ran + PASSED before QA (correct loop order).
**Verdict: PASS — ship-ready.** Zero Critical, zero Warning, 2 non-blocking Suggestions.

## Scorecard

| Axis | Verdict |
|---|---|
| Completeness | PASS — every master AC + a-AC-1..6 + b-AC-1..7 + D-1..D-5 traced to code |
| Correctness | PASS — D-3 faithful refactor verified line-by-line; teeth-proof genuine |
| Alignment | PASS — implementation matches PRD intent; no scope drift |
| Gaps | 1 Suggestion (FR-4 partial-outage preflight breadth), 1 Suggestion (dead `isRetry` field) |

## Master AC verification

- **AC-1 / AC-4 — VERIFIED.** Merge gate = `quality-gate` (Node 22/24) + `windows-smoke` only. `integration-push-soft` is `continue-on-error: true` (push); `integration-nightly` is hard (schedule + opt-in dispatch). No job `needs` the live/stress jobs → they can never block a merge. `gate`→`has_token` skip-safe preserved.
- **AC-2 (D-3 faithful refactor, the headline) — VERIFIED.** `expect()` count byte-identical old↔new per refactored file (compaction 42, sources-purge 5, golden-path 28, retention 9, skills-write 7). Zero correctness assertions (value / tenancy / idempotency / no-data-loss / graceful-degradation) deleted or weakened; only immediacy (tight timeouts / exact-count-now) relaxed, all via the shared `readConverged` seam (no bespoke poll loops added). `teeth-proof-live.itest.ts` genuinely proves WRONG-VALUE, BROKEN-TENANCY (foreign-workspace read must not surface the row), and LOST-WRITE would be caught.
- **AC-3 — VERIFIED.** `_infra-skip.ts` neutralizes only transient-class failures (`isTransientResult` verbatim); a non-transient `query_error` (42P01/400/401/403) stays RED. `action.yml` mapping: marker present → exit 0 + `::warning::` (neutral); marker absent + fail → exit 1 (real red). Infra-neutral never masks a real regression.
- **AC-5 / b-AC-1..7 — VERIFIED.** `deeplake:stress` + the `workflow_dispatch` job emit latency p50/p95/p99/max by stmt-kind, error-rate by HTTP status, convergence-time distribution, throughput, error-vs-concurrency, raw-vs-effective; human summary + `StressReport` JSON (schemaVersion 1); throwaway table + DROP; no secret (redaction asserted); never on push, never gates.
- **AC-6 — VERIFIED.** Dials zod-clamped; `mulberry32` seeded; reproducibility tested (same seed → identical SQL stream).
- **AC-7 — VERIFIED.** Deterministic gate green + unit-count-stable; stress is `workflow_dispatch`-only.

## Gates (run by QA)
typecheck clean · build clean · `test` 2275 passed / 1 fail (the known `sources/api.test.ts` a-AC-2 parallel-load flake — passes in isolation, outside the PRD-034 surface) · audit:sql clean · audit:openclaw clean · dup 0.54% (< 7).

## Suggestions (non-blocking)
- **S-1:** `neutralizeIfInfraDegraded` preflight wired into 2 representative itests (compaction, sources-purge) as the demonstration; the helper/sentinel/reporter/CI-mapping are general and complete (the run-level signal — the binding FR-4 requirement — is satisfied). Acceptable v1 per FR-4's "where feasible"; a follow-up could widen the preflight to the remaining gated itests.
- **S-2:** `AttemptSample.isRetry` is recorded but unused in the report path (raw-vs-effective derives from `isTerminalAttempt`). Harmless dead field / stale comment; optional cleanup.

## Live close-out (D-5)
`npm run deeplake:stress` ran live (`run=t520701900`): error-rate-vs-concurrency c=1 2.0% / c=4 37.7% / c=8 46.2%; raw 31.7% (500s+other), 20.4% post-retry; insert p99 1050ms / select p99 1524ms / DDL p95 2882ms; convergence 138ms. Quantifies why live CI red on weather and is the reproducible vendor artifact.
