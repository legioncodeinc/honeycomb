# QA Report: PRD-029 follow-on — `/api/status.reasons` (portal Memory Formation fail-closed fix)

**Plan document:** source plan supplied in-band (PR #248 context); tied to `library/requirements/completed/prd-029-degradation-observability/`
**Audit date:** 2026-07-05
**Base branch:** `main`
**Head:** `fix-lease-discovery-on-paginated-scan` (uncommitted working-tree diff, PR #248)
**Auditor:** quality-worker-bee
**Ordering:** `security-worker-bee` ran on this diff (zero findings) — no ordering violation.

## Summary

**PASS — ship.** The additive `reasons` block on `GET /api/status` is single-sourced from the identical `publicHealthDetail(healthDetail(), config.mode)` seam that `/health` already uses (server.ts:342 vs server.ts:366 — byte-identical expression), so the two surfaces are structurally incapable of drift. Mode-gating, additivity, the fail-closed `memory.provider: "unconfigured"` signal, and the bare-daemon shape preservation are all proven by the +5 tests, and every gate is green (tsc 0, vitest 62/62, dup 0, audit:sql 0). One minor and one nit, both non-blocking and both pre-existing in the PRD-029 line.

## Scorecard

| Category      | Status | Notes |
|---------------|--------|-------|
| Completeness  | ✅ | All five verify bullets addressed; `memory`/`embeddings`/`portkey` always present in `reasons`. |
| Correctness   | ✅ | Seam is a verbatim copy of `/health`; `toEqual` deep-equality test proves no recompute/drift. |
| Alignment     | ✅ | Reuses imported `publicHealthDetail` + injected `healthDetail` thunk; no new seam, no boundary crossing. |
| Gaps          | ✅ | Additive spread guarded by `detail?.reasons !== undefined`; bare-daemon path covered by test. |
| Detrimental   | ✅ | Purely additive (+95 lines, 0 deletions); pre-029 `/api/status` field-level test still green; no regression. |

## Critical Issues (must fix)

None.

## Warnings (should fix)

None.

## Suggestions (consider improving)

- [ ] **`schema` reason is hard-wired `ok` (missing-table signal never surfaced)**, `src/daemon/runtime/health.ts:381`

  `buildHealthDetail` emits `schema: inputs.schemaMissingTable === true ? "missing_table" : "ok"`, and no production caller passes `schemaMissingTable`, so `/api/status.reasons.schema` is effectively always `"ok"`. This is a deliberate, documented deferral from the parent PRD-029 (health.ts:27-30, "conservative — never a false missing_table"), not introduced by this diff, and the portal's Memory Formation control does not read `schema`. Non-blocking; noted for continuity.

- [ ] **`reasons` typed as `any` in the new tests**, `tests/daemon/runtime/server.test.ts:126,140,153,169,181`

  The five added tests cast the parsed body to `any` (`(await res.json()) as any`), matching the pre-existing a-AC-3 test convention in the same file (line 88). Consistent with the suite, so no drift; a typed `HealthDetail`-shaped assertion would catch shape regressions at compile time. Nit only.

## Plan Item Traceability

| #    | Plan Requirement | Status | Implementation Location | Notes |
|------|------------------|--------|-------------------------|-------|
| V-1  | `/api/status.reasons` includes `memory {enabled, provider}`, `embeddings`, `portkey` | ✅ | `src/daemon/runtime/health.ts:377-424`; asserted `tests/daemon/runtime/server.test.ts:127-131` | `memory`/`embeddings`/`portkey` always emitted; `memory` present when composition root wires it (test supplies it). |
| V-2  | Provider-unconfigured daemon → `memory.provider: "unconfigured"` (fail-closed signal) | ✅ | `src/daemon/runtime/health.ts:420`; test `tests/daemon/runtime/server.test.ts:147-158` | Portal reads this to fail-closed the Memory Formation control. |
| V-3  | Configured daemon → `memory.provider: "configured"` | ✅ | `src/daemon/runtime/health.ts:420`; test `server.test.ts:129` | |
| V-4  | No drift: `/api/status.reasons` deep-equals `/health.reasons` (single-sourced) | ✅ | `src/daemon/runtime/server.ts:366` (identical to `:342`); test `server.test.ts:143-145` `expect(statusBody.reasons).toEqual(healthBody.reasons)` | Same `healthDetail()` thunk + same `publicHealthDetail` call. Structurally cannot recompute. |
| V-5  | Mode-gating: team/hybrid strips `reasons`; local includes it | ✅ | `src/daemon/runtime/health.ts:438-442` (`publicHealthDetail`); test `server.test.ts:160-172` loops team+hybrid | Identical gate to `/health`; `local` returns full detail, non-local drops `reasons`. |
| V-6  | Additive: existing `/api/status` fields (version/config/providers/tenancy/catalog) unchanged | ✅ | `src/daemon/runtime/server.ts:367-396` (spread appended last, +0 deletions) | Pre-029 a-AC-3 test (`server.test.ts:82-96`) still green in the 23-test run. |
| V-7  | Bare daemon (no `healthDetail` seam) keeps prior shape | ✅ | `src/daemon/runtime/server.ts:366,395` (`healthDetail !== undefined` guard + `detail?.reasons` guard); test `server.test.ts:174-179` | `reasons` undefined; `catalog.tableCount > 0` preserved. |
| V-8  | No existing `/api/status` or `/health` test broke | ✅ | vitest `server.test.ts` 23 pass, `health.test.ts` 33 pass, `health-dormancy.test.ts` 6 pass | 62/62 across the three suites. |

## Gate Results (real numbers)

| Gate | Command | Result |
|------|---------|--------|
| Typecheck | `npx tsc --noEmit` | ✅ exit 0, no errors |
| Duplication | `npm run dup` (jscpd, threshold 7) | ✅ exit 0 — 34 clones total (0.43% lines / 0.66% tokens), all pre-existing in unrelated files (scope.ts, prune.ts, deeplake-stress.ts, converge.ts, install.ts, deeplake-issuer.ts); none in the diff |
| SQL safety | `npm run audit:sql` | ✅ exit 0 — 302 files scanned, every interpolation routes through an escaping helper |
| Targeted tests | `npx vitest run server.test.ts health.test.ts health-dormancy.test.ts` | ✅ exit 0 — 3 files, 62/62 tests passed |

Known-flaky suites (`tests/hooks/runtime/hook-runtime.test.ts`, `assemble.test.ts:508` tenancy-timeout) were not in scope and not run per plan instruction.

## Files Changed

- `src/daemon/runtime/server.ts` (M) — `/api/status` handler computes `publicHealthDetail(healthDetail(), config.mode)` at :366 (verbatim `/health` seam) and additively spreads `...(detail?.reasons !== undefined ? { reasons: detail.reasons } : {})` at :395; +14 lines, 0 deletions.
- `tests/daemon/runtime/server.test.ts` (M) — +5 tests in a new `PRD-029 /api/status carries the additive, mode-gated reasons` describe block: local-body shape, `/health` deep-equality, unconfigured fail-closed, team/hybrid strip, bare-daemon shape; +81 lines, 0 deletions.

## Overall Verdict

**SHIP.** All eight plan requirements verified, all four gates green, zero blockers, zero majors. The single Suggestion (`schema` hard-wired `ok`) is a documented parent-PRD deferral, and the nit (`any`-typed test bodies) matches the file's existing convention. The single-sourcing guarantee is the load-bearing claim and it is structural, not merely tested — the `/api/status` expression is a character-for-character copy of the `/health` expression reading the same injected thunk.
