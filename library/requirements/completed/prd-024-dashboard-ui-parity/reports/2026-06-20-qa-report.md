# QA Findings Report — PRD-024 Dashboard UI Parity

> Auditor: `quality-worker-bee` · Date: 2026-06-20 · Branch: `prd-024-dashboard-ui-parity`
> Order: ran AFTER `security-worker-bee` (clean: no Critical/High; D-4 invariants PASS) — correct sequence.
> Method: graded the CODE against the SOURCE PRD (8 ACs / D-1..D-5), not the ledger. Ran all gates.

## Verdict: **VERIFIED** (pending AC-8 orchestrator live run)

All 6 implementation ACs (AC-1..AC-6) PASS against the shipped code. AC-7 (security) was closed clean by the prior bee; the gates are re-confirmed green here. AC-8 (assembled-daemon live render + screenshot) is the orchestrator's remaining manual step — the gated itest scaffolding for it is present and the unit/DOM proof is complete. No OPEN blockers. Two non-blocking Suggestions noted (S-1, S-2).

## Gate exit codes (run this session, repo root)
| Gate | Exit | Evidence |
|------|------|----------|
| `npm run build` (`tsc && esbuild`) | **0** | emits `1 daemon + 1 dashboard-web + 5 hook-harness + 1 OpenClaw + 1 MCP + 4 SDK + 1 CLI + 1 embed-daemon @ 0.1.0` |
| `npm run ci` (typecheck + jscpd + vitest + audit:sql) | **0** | 179 files, **1772 passed / 5 skipped**; audit:sql "every SQL interpolation routes through an escaping helper" (162 files) |
| `npm run audit:sql` | **0** | (folded into ci above; clean) |
| `npm run audit:openclaw` | **0** | "no findings. Bundle is clean against ClawHub's static-analysis rules" |
| `tests/daemon/storage/invariant.test.ts` | **0** | 3/3 passed |

## Per-AC verdict
| AC | Verdict | Key evidence (file:line) |
|----|---------|--------------------------|
| AC-1 The look + production-clean bundle | **PASS** | `host.ts:97-115` shell (`#root` + DS CSS link + `<script type=module>`, no CDN/Babel); `esbuild.config.mjs:136-148` browser entry, React bundled in, `minify`; built `daemon/dashboard-app.js` greps **0** for `unpkg`/`@babel/standalone`/`text/babel`; kit's `index.html:10-15,33` HAD all four forbidden markers — they are gone. Asserted: `build-output.test.ts:38-62`, `host.test.ts:85-93`, `app.test.tsx:128-154`. |
| AC-2 Live data, no canned | **PASS** | `app.tsx:178-198` `Promise.all` hydrates settings/kpis/sessions/rules/skills/graph from live endpoints; `wire.ts:255-275` per-endpoint zod fetch; bundle greps **0** for `data.js` canned values (`deeplake-query-builder`/`Never deploy from main`); no `data.js`/`window.*` import in `src/dashboard/web/*` (only doc-comments disclaiming it). Empty states: `panels.tsx:76-77,146-147,187-195`; asserted `app.test.tsx:172-190`. |
| AC-3 Recall | **PASS** (one fidelity caveat, S-1) | recall bar Enter+button → `app.tsx:235-246` → `wire.ts:276-300` POST `/api/memories/recall`; wire `RecallResponseSchema` (`wire.ts:132-136`) matches the real handler response `{hits,sources,degraded}` (`memories/api.ts:156-161`). MemoryCard renders snippet/source/score/scope/verified (`primitives.tsx:332-422`). Asserted `app.test.tsx:193-216`. |
| AC-4 Live log | **PASS** | `app.tsx:201-214` polls `/api/logs?limit=8` @2.5s, stops on unmount; `wire.ts:330-334` `formatLogLine` renders time+method+path+status only (no header/token). Asserted no-secret `app.test.tsx:219-230`. |
| AC-5 Connectivity | **PASS** | `app.tsx:217-232` polls real `/health` @5s; down → whole-view swap to `ConnectivityBanner` (`app.tsx:270-286`); Retry re-probes + re-hydrates (`app.tsx:276-282`). Driven by real `/health` (kit's demo toggle dropped). Asserted `app.test.tsx:233-273`. |
| AC-6 Dream now (real) | **PASS** | Frontend `app.tsx:249-264` POSTs `/api/diagnostics/dream`, honest skip log (no fake forever-spinner). Backend `dreaming/api.ts:202-231` reuses 009a `checkAndEnqueueDreaming` (non-blocking enqueue), 202 ack, fail-soft `unavailable`. Wired once with daemon's own queue: `assemble.ts:427-438`; gated local-only + once-each: `assemble.test.ts:148-162,197-216,235-239`. Backend suite `dreaming/api.test.ts` (10 cases). |
| AC-7 Security | **PASS** (prior bee + re-confirmed) | host local-mode-only `assemble.ts:401-403`; no-secret shell `host.test.ts:95-102`; no-secret ack `dreaming/api.test.ts:150-162`; gates green above. |
| AC-8 Live verification | **PENDING (orchestrator)** | Gated itest present (`tests/integration/dashboard-logs-live.itest.ts:121-143` asserts assembled daemon serves production-clean shell). Manual screenshot/curl against a real assembled daemon is the orchestrator's step; not a code gap. |

## Decisions D-1..D-5
- **D-1 reuse DS, bundle clean** — PASS (esbuild browser entry, React/JSX compiled at build time; 0 CDN/Babel in shell + bundle).
- **D-2 live data, no canned** — PASS (live fetch + zod; 0 `data.js` values in bundle).
- **D-3 real dream trigger** — PASS (009a seam reused, non-blocking enqueue, real queue).
- **D-4 security unchanged** — PASS (host local-gated, XSS-safe shell, no token in page/data/logs/trigger; audit:sql + audit:openclaw green).
- **D-5 reuse not fork** — PASS (wire schemas mirror `contracts.ts`; primitives/panels ported verbatim from `assets/components` + `components.jsx`; no new data contracts).

## Test quality review (no weakened/tautological assertions found)
- `app.test.tsx` drives the **real** wire layer (`createWireClient({fetchImpl})`), only `fetch` is mocked — the zod boundary is genuinely exercised. Assertions check concrete values (`"1,284"`, `"deploy/prd-022"`, `"1.00"`), POST method (`:91,215`), view-swap + restore (`:253-271`), honest skip (`:309-314`). No `expect(true)`, no mocked-away production path.
- `build-output.test.ts` reads the **actual** built `daemon/dashboard-app.js` and asserts 0 forbidden markers + no bare `from "react"` (`:54-62`). The bundle assertion `skipIf(!built)` is sound — the always-on shell assertion covers the un-built case and the live itest covers the served path.
- `dreaming/api.test.ts` mounts a **real** daemon, uses a recording fake trigger to assert "fired exactly once with `{agentId:'default'}'`" (`:84-95`), fail-closed edge (`:131-148`), no-secret ack incl. job-id (`:150-162`), non-blocking bound (`:175-186`).
- `assemble.test.ts` asserts the dream seam is wired with the daemon's **own** queue (`:159-160`) and gated local-only (`:235-239`, `:258-274`).

## Findings (non-blocking)
- **S-1 (Suggestion) — AC-3 `score`/`scope`/`verified` are derived client-side, not carried on the wire.** The PRD AC-3 phrasing says cards render those fields "from the wire," but the recall engine genuinely returns only `{source,id,text}` (`memories/api.ts:156-161`, `recall.ts:69-77`). `wire.ts:286-295` derives `score = max(0, 1 - i*0.06)` (positional, not a real relevance score), `scope` from a source-name heuristic, `verified = source==="memories"`. This is the honest best-effort given the backend and is documented in `wire.ts:120-126`; it is NOT a fabricated-data violation. Recommend a one-line PRD note that score/scope/verified are presentation-derived until the recall engine surfaces real relevance, so the wording and the code agree. Owner: `library-worker-bee` (PRD author), not a code fix.
- **S-2 (Suggestion) — PRD endpoint list lists `/api/diagnostics/graph`; the served route is `/api/graph`.** PRD line 24 and the ledger reference `/api/diagnostics/graph`, but the daemon serves the graph view-model at `/api/graph` (`dashboard/api.ts:90-91,281-288`) and the wire correctly targets `/api/graph` (`wire.ts:38`). The implementation is correct against the real daemon; the PRD text is the stale one. Recommend correcting the PRD endpoint table. Owner: `library-worker-bee`.

## Scope notes
- Work is uncommitted (working-tree changes + untracked files vs `main`); no `git add`/commit/push performed, per instructions. New source: `src/dashboard/web/{wire.ts,primitives.tsx,panels.tsx,app.tsx,main.tsx}`, `src/daemon/runtime/dashboard/web-assets.ts`, `src/daemon/runtime/dreaming/api.ts`. New tests: `tests/dashboard/web/{app.test.tsx,build-output.test.ts}`, `tests/daemon/runtime/dreaming/api.test.ts`. Changed: `host.ts`, `assemble.ts`, `esbuild.config.mjs`, `tsconfig.json`, `vitest.config.ts`, `package.json`, plus the two seam tests + the live itest.
