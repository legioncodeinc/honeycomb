# QA Report: PRD-016 Skillify

**Plan document:** `library/requirements/in-work/prd-016-skillify/` (index + 016a/016b/016c)
**Audit date:** 2026-06-18
**Base branch:** `main`
**Head branch:** `prd-016-skillify`
**Auditor:** quality-worker-bee
**Ordering:** `security-worker-bee` report confirmed at `library/requirements/in-work/prd-016-skillify/reports/2026-06-18-security-report.md` (0 Critical, 1 High FIXED, 1 Medium documented, 1 Low documented). Ordering invariant satisfied.

## Summary

PRD-016 Skillify is **CLEAN TO SHIP**. All 21 ACs (18 sub-ACs across 016a/016b/016c + 3 index roll-ups) are satisfied by named, non-tautological, unskipped Vitest tests that make meaningful assertions. The four highest-risk invariants — append-only skills write (no UPDATE ever emitted), no-shell gate CLI (spawn args array + `shell:false`), symlink path-safety (hostile name/author cannot traverse out of the canonical root), and through-daemon thin-client (invariant test passes 3/3) — are verified by code inspection and are backed by dedicated test assertions. The security-worker-bee HIGH fix (`redactSecrets` at the mine boundary) is confirmed present and verified by 2 regression tests planted in `miner.test.ts`. The deferred worker/hook-assembly wiring (daemon job running `mine` on a Stop signal; session-start auto-pull hook) is honestly deferred and documented in `CONVENTIONS.md §11` and the EXECUTION_LEDGER, matching the posture of PRD-008..015. Gate results: `npm run ci` exit 0 (1105 passed / 4 skipped), `npm run build` exit 0, `npm run audit:openclaw` exit 0, `npm run audit:sql` exit 0.

## Scorecard

| Category     | Status | Notes |
|--------------|--------|-------|
| Completeness | ✅     | All 21 ACs traced to named test assertions; 0 gaps |
| Correctness  | ✅     | Key behaviors verified by code inspection: lock in `finally`, KEEP floor ≥3, sanitizeBody funnel, append-only INSERT (no UPDATE method on SkillStore), path-safety sanitizer |
| Alignment    | ✅     | 016c placed in `src/daemon-client/skillify/` (structurally enforces thin-client invariant); re-exported from `src/daemon/runtime/skillify/install.ts`; vocabulary matches plan |
| Gaps         | ✅     | Security HIGH fix present + 2 regression tests; M-1/L-1 documented in security report (not silent); deferred wiring documented honestly |
| Detrimental  | ✅     | jscpd at 0.4% tokens (threshold 7%, exit 0); no regressions in existing test suite; no leftover debug code found; the one clone touching new skillify code (`skills-write.ts` ↔ `lifecycle.ts` pattern) is an expected structural parallel in the append-only subsystem |

## Critical Issues (must fix)

None.

## Warnings (should fix)

None.

## Suggestions (consider improving)

- [ ] **jscpd clone: `skills-write.ts:139-151` duplicates `sources/lifecycle.ts:279-299`**, `src/daemon/runtime/skillify/skills-write.ts:139-151`

  The `appendOnlyInsert` + `withHeal` pattern used in `skills-write.ts` (12 lines / 149 tokens) is structurally identical to the same pattern in `sources/lifecycle.ts`. The duplication is below the 7% jscpd threshold (currently 0.4% total) and the CI gate passes, but if the project adds more append-only writers the combined clone count may eventually approach the threshold. Consider extracting a shared `healedAppend` helper in a future refactor cycle. Not blocking.

- [ ] **Deferred wiring not yet linked from PRD-016 index sub-feature status**, `library/requirements/in-work/prd-016-skillify/prd-016-skillify-index.md:30-33`

  The sub-PRD status column still reads "Draft" for all three sub-features. The CONVENTIONS.md §11 and EXECUTION_LEDGER document the deferred worker/hook-assembly correctly, but the index PRD's sub-feature table was not updated to reflect the implemented state (e.g. "Implemented — daemon wiring deferred"). Not a functional gap; a documentation hygiene item for the library-worker-bee on close-out.

## Plan Item Traceability

| #         | Plan Requirement                                                                                   | Status | Implementation Location                                                                                                         | Notes |
|-----------|---------------------------------------------------------------------------------------------------|--------|---------------------------------------------------------------------------------------------------------------------------------|-------|
| a-AC-1    | Last 10 in-scope sessions past watermark → pairs extracted (drop tool/thinking), cap 2000/pair + 40000 total, exclude trigger session | ✅ | `miner.test.ts:111` ("a-AC-1 extracts pairs…"); `miner.ts:347` `extractPairsFromRows`; cap constants `MAX_PAIR_CHARS=2000`, `MAX_BATCH_CHARS=40000` at `miner.ts:66-68` | Trigger exclusion asserted via SQL NOT LIKE in a-AC-4 test + end-to-end via fake fetcher |
| a-AC-2    | Gate returns exactly KEEP/MERGE/SKIP; KEEP requires ≥3 exchanges                                 | ✅ | `miner.test.ts:155` ("a-AC-2 the gate returns exactly one of KEEP/MERGE/SKIP…"); `miner.ts:488` `normalizeVerdict`; `miner.ts:495` KEEP floor enforced | Downgrade-to-SKIP for <3-pair batch proven at `miner.test.ts:165-173` |
| a-AC-2+   | No-shell gate CLI (shell:false, args array)                                                        | ✅ | `miner.test.ts:182` ("a-AC-2 the host-CLI gate shells out with an args array (shell:false)…"); `miner.ts:546` `spawn(spec.command, [...spec.args], { shell: false })` | Assertion verifies injection payload stays in stdin body, never in args |
| a-AC-3    | Stop-counter ≥ N → reset + run worker; session-end unconditional                                  | ✅ | `miner.test.ts:219` ("a-AC-3 the stop-counter reaching N resets…"); `miner.ts:743-757` `evaluateTrigger` reusing `TurnCounters` / `tryStopCounterTrigger` | `skillifyEveryNTurns` env-read asserted at `miner.test.ts:241-243` |
| a-AC-4    | Scope `team` + team list → `author IN (<team>)` escaped via `sqlStr`                              | ✅ | `miner.test.ts:247` ("a-AC-4 scope team…"); `miner.ts:186-191` per-value `sLiteral`; assertion pattern `/author IN \('alice', 'o''brien'\)/` at `miner.test.ts:259` | Single-quote doubling for `o'brien` verified |
| a-AC-5    | Concurrent run → lock suppresses second                                                            | ✅ | `miner.test.ts:274` ("a-AC-5 a concurrent run…") in-memory lock; `miner.test.ts:309` ("a-AC-5 the file worker lock atomically suppresses…") real `O_EXCL` lock; `miner.ts:803-808` `mine` acquire path | Lock idempotent release also asserted |
| a-AC-6    | Gate CLI > 120s → abort, no verdict, lock released in `finally`                                   | ✅ | `miner.test.ts:332` ("a-AC-6 a gate exceeding the timeout…"); `miner.ts:819` `finally { handle.release() }`; injectable `gateTimeoutMs` drives fast path | Post-abort fresh acquire proves lock freed |
| b-AC-1    | KEEP → SKILL.md w/ provenance frontmatter + append-only version row (NEVER UPDATE)                | ✅ | `skills-write.test.ts:96` ("b-AC-1 KEEP writes a SKILL.md…"); `skills-write.test.ts:127` SQL invariant (INSERT asserted, UPDATE count=0); `skills-write.ts:166-189` `appendVersion` | Live itest: `skills-write-live.itest.ts` orchestrator-confirmed 3/3 |
| b-AC-1+   | Second KEEP for same (name, author) → version N+1, prior retained                                | ✅ | `skills-write.test.ts:162` ("b-AC-1 a second KEEP…"); assertions on `store.appended.length === 2`, versions 1 and 2 | |
| b-AC-2    | Any verdict → watermark advances to oldest mined session date                                     | ✅ | `watermark.test.ts:23` ("b-AC-2 advances the watermark to the OLDEST mined session date…"); `watermark.ts` `advance()` returns `min(dates)` | Monotonicity (newer batch never moves watermark forward) asserted at `watermark.test.ts:36` |
| b-AC-3    | MERGE absent target → `writeNewSkill` fallback, body preserved                                    | ✅ | `skills-write.test.ts:183` ("b-AC-3 MERGE whose target is absent…"); `outcome.mergeFellBack === true`; body contains "Recovered body" | |
| b-AC-4    | Cross-author merge → scope `me`→`team`                                                            | ✅ | `skills-write.test.ts:210` ("b-AC-4 a cross-author merge promotes…"); `outcome.scope === "team"`; frontmatter `scope: team` asserted from file | |
| b-AC-5    | `install=project` → `<cwd>/.claude/skills/`; `install=global` → `~/.claude/skills/`              | ✅ | `skills-write.test.ts:243` ("b-AC-5 install=project lands under…"); exact path assertions for both install targets | |
| b-AC-6    | Any successful write → through daemon (not direct DeepLake)                                        | ✅ | `skills-write.test.ts:261` ("b-AC-6 every successful write goes through the SkillStore seam…"); real store over `FakeDeepLakeTransport` proves dispatch path; `SkillStore` has no `update` method by construction (`contracts.ts:279-296`) | |
| c-AC-1    | `skillify pull` → `~/.claude/skills/<name>--<author>/SKILL.md` + symlinks to every agent root    | ✅ | `install.test.ts:85` ("c-AC-1 pull writes…"); `skillify.test.ts:49` ("c-AC-1 `skillify pull` writes the canonical SKILL.md…"); `install.ts:77` `pull()` | Symlink existence asserted in `it.skipIf(!CAN_SYMLINK)` guard at `install.test.ts:105` |
| c-AC-2    | Auto-pull skip-if-local-newer + 5s-timeout-swallow                                                | ✅ | `install.test.ts:144` (skip equal version); `install.test.ts:179` (error swallowed); `install.test.ts:194` (timeout 20ms < 200ms store, returns null); `install.ts:91` `localVersionAtLeast` + `autoPull:143-147` try/catch swallow | |
| c-AC-3    | `HONEYCOMB_AUTOPULL_DISABLED=1` → auto-pull does not run                                          | ✅ | `install.test.ts:231` ("c-AC-3 HONEYCOMB_AUTOPULL_DISABLED=1…"); `client.calls.count === 0` asserted; `install.ts:133` kill-switch check | |
| c-AC-4    | Unauthenticated → auto-pull skips silently                                                         | ✅ | `install.test.ts:249` ("c-AC-4 an UNAUTHENTICATED session…"); `client.calls.count === 0`; `install.ts:136` auth check | |
| c-AC-5    | Global-install pull → symlink in each detected agent root → canonical dir                         | ✅ | `install.test.ts:123` ("c-AC-5 a global-install pull fans a symlink into EACH detected agent root…"); 2 skills × 3 roots = 6 symlinks asserted | |
| c-AC-6    | Any pull → queries store through daemon (not direct DeepLake)                                      | ✅ | `install.test.ts:261` ("c-AC-6 a pull reaches the store ONLY through the seam…"); `skillify.test.ts:71` ("c-AC-6 the pull reaches the store ONLY through the injected client seam"); `pull-client.ts` dispatches via `DaemonDispatch` seam; `install.ts` in `src/daemon-client/` structurally forbids storage-client import | Daemon dispatch path proven via `install.test.ts:271` with `createFakeDaemonDispatch` |
| AC-1 (idx)| Stop-counter → reset + worker; session-end unconditional                                           | ✅ | Roll-up of a-AC-3 | |
| AC-2 (idx)| Gate KEEP only for ≥3-exchange non-obvious not-covered                                            | ✅ | Roll-up of a-AC-2 | |
| AC-3 (idx)| KEEP → append-only version row (never UPDATE) + watermark                                         | ✅ | Roll-up of b-AC-1, b-AC-2 | |
| NG-1      | Session-capture pipeline (consumed, not owned)                                                     | 🟦 | Correctly deferred — sessions table consumed read-only via `createSessionFetcher` | |
| NG-2      | Gate model definition (shells out to host CLI)                                                     | 🟦 | Correctly deferred — gate is a seam (`GateCli`) shelled out to host harness | |
| NG-3      | Cross-org skill sharing                                                                            | 🟦 | Tenancy enforced via `QueryScope`; no cross-org path exists | |
| DEFERRED  | Daemon worker assembly (job running `mine` on Stop signal)                                         | 🟦 | Documented in `CONVENTIONS.md §11` and EXECUTION_LEDGER; seam (`mine`, `evaluateTrigger`) is fully implemented and tested | |
| DEFERRED  | Session-start auto-pull hook wiring                                                                | 🟦 | Documented in `CONVENTIONS.md §11` and EXECUTION_LEDGER; `autoPull` is fully implemented and tested | |
| SEC H-1   | `redactSecrets` at mine boundary (FIXED by security-worker-bee)                                   | ✅ | `miner.ts:315` `redactSecrets()`; `miner.ts:326` `sanitizeBody = redactSecrets(stripThinking(text))`; `miner.ts:381,389` both prompt and answer pass through `sanitizeBody`; 2 regression tests at `miner.test.ts:371,393` | Both tests assert secret material absent from pairs; benign text survives (low FP) |
| SEC M-1   | Pattern-based redaction does not cover arbitrary high-entropy secrets                              | 🟦 | Documented in security report as residual hardening; entropy-heuristic follow-up noted | |
| SEC L-1   | Symlink fan-out swallows all errors silently                                                        | 🟦 | Documented in security report; intentional for win32 privilege degradation; debug-log suggestion noted | |

## Files Changed

- `library/ledger/EXECUTION_LEDGER-prd-016.md` (A), PRD-016 execution ledger with 21 AC statuses, decisions, watchdog, and deferred wiring documentation
- `library/requirements/in-work/prd-016-skillify/prd-016-skillify-index.md` (M), moved from backlog to in-work; sub-feature status column unchanged at "Draft"
- `library/requirements/in-work/prd-016-skillify/prd-016a-skillify-trace-miner.md` (R), renamed from backlog
- `library/requirements/in-work/prd-016-skillify/prd-016b-skillify-skills-writes.md` (R), renamed from backlog
- `library/requirements/in-work/prd-016-skillify/prd-016c-skillify-skill-install.md` (R), renamed from backlog
- `library/requirements/in-work/prd-016-skillify/reports/2026-06-18-security-report.md` (A), security-worker-bee findings report (0 Critical, 1 High FIXED)
- `src/cli/skillify.ts` (A), `honeycomb skillify pull` CLI entry point; thin client dispatching through daemon seam
- `src/daemon-client/skillify/contracts.ts` (A), thin-client contracts: `PulledSkill`, `SkillPullClient`, `AgentRootDetector`, `AuthCheck`
- `src/daemon-client/skillify/index.ts` (A), thin-client barrel re-exporting pull/autoPull/fakes
- `src/daemon-client/skillify/install.ts` (A), `pull()` + `autoPull()` implementation; path safety via `sanitizeSegment`; symlink fan-out
- `src/daemon-client/skillify/pull-client.ts` (A), `createDaemonPullClient` and `createFakeSkillPullClient`; highest-version SELECT dispatched through daemon
- `src/daemon/runtime/skillify/CONVENTIONS.md` (A), conventions for the skillify subsystem including §11 deferred wiring
- `src/daemon/runtime/skillify/contracts.ts` (A), shared contracts: `MinedPair`, `GateVerdict`, `GateCli`, `Skill`, `SkillStore`, `WatermarkStore`, seams
- `src/daemon/runtime/skillify/index.ts` (A), barrel exporting all of 016a/016b/016c
- `src/daemon/runtime/skillify/install-target.ts` (A), `createFsInstallTarget` — filesystem seam for SKILL.md writes (b-AC-5)
- `src/daemon/runtime/skillify/install.ts` (A), thin re-export of daemon-client pull/autoPull (preserves through-daemon invariant)
- `src/daemon/runtime/skillify/miner.ts` (A), 016a: `extractPairsFromRows`, `runGate`, `normalizeVerdict`, `evaluateTrigger`, `mine`; `redactSecrets`/`sanitizeBody` (security HIGH fix); `createFileWorkerLock`; `createSessionFetcher`
- `src/daemon/runtime/skillify/skills-write.ts` (A), 016b: `createSkillStore` (append-only INSERT, no update method), `writeSkill`, `writeNewSkill`, `mergeSkill`, `renderSkillMarkdown`
- `src/daemon/runtime/skillify/watermark.ts` (A), `createWatermarkStore` — advances to oldest mined session date (b-AC-2)
- `tests/cli/skillify.test.ts` (A), CLI tests: c-AC-1 + c-AC-6 named + usage/verb edge cases (5 tests)
- `tests/daemon-client/skillify/install.test.ts` (A), install tests: c-AC-1..6 + path-safety (14 tests)
- `tests/daemon/runtime/skillify/miner.test.ts` (A), miner tests: a-AC-1..6 + no-shell gate + security redaction (11 tests)
- `tests/daemon/runtime/skillify/skills-write.test.ts` (A), skills-write tests: b-AC-1..6 + SKIP + renderSkillMarkdown + catalog reconcile (10 tests)
- `tests/daemon/runtime/skillify/watermark.test.ts` (A), watermark tests: b-AC-2 + monotonicity + SKIP advance + per-project isolation + path-traversal containment (5 tests)
- `tests/integration/skills-write-live.itest.ts` (A), opt-in live itest: v1→v2 append-only write, highest-version read returns v2; throwaway-table isolation; orchestrator-confirmed 3/3
