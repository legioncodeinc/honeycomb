# QA Findings Report — PRD-025 Semantic recall on by default

> Audited: 2026-06-21 · Branch: `prd-025-semantic-recall-default` · Auditor: quality-worker-bee
> Source PRD: `library/requirements/in-work/prd-025-semantic-recall-default/prd-025-semantic-recall-default-index.md` (7 ACs, D-1..D-6)
> Ledger: `library/ledger/EXECUTION_LEDGER-prd-025.md`
> Ordering: security-worker-bee ran FIRST and is clean (1 High remediated — embed child no longer inherits the DeepLake token; no Critical). quality is correctly SECOND. ✅

## Verdict: **VERIFIED**

All 7 acceptance criteria are met by the shipped code (graded against the code, not the ledger). Every gate is green. No weakened, tautological, or mocked-away-production-path tests were found. The one live behavioral bar (AC-4) is proven by a genuine differential gated itest; its live execution is attested by the orchestrator's Wave-3 run and the itest skips cleanly in a token-less environment (confirmed in this audit). No OPEN items.

---

## Per-AC scorecard

| AC | Criterion (abbrev) | Status | Key evidence (file:line) |
|----|--------------------|--------|--------------------------|
| AC-1 | Default-ON opt-OUT + daemon owns/supervises embed daemon + zero-config first-run | **PASS** | `embed-client.ts:172`; `embed-supervisor.ts:256-433`; `assemble.ts:580`; `ensure-embed-deps.mjs` |
| AC-2 | Store + capture seams default to REAL `createEmbedAttachment` | **PASS** | `assemble.ts:611,465,411`; live read-back per ledger Wave-3 |
| AC-3 | Recall runs `<#>` arm via `vectorSearch`; `degraded` honest; old line-255 GONE | **PASS** | `recall.ts:439`; `api.ts:198-200`; both branches unit-proven |
| AC-4 | Gated itest: semantic surfaces lexical-MISS query, lexical does not | **PASS** | `semantic-recall-live.itest.ts:220-273`; skips cleanly token-less (verified) |
| AC-5 | Kill→degrade→restart: recall honest degrade + supervisor stop()/restart() | **PASS** | `embed-supervisor.ts:387-431`; supervisor.test.ts AC-5 cases |
| AC-6 | Dim invariant: non-768/malformed → NULL + lexically recallable | **PASS** | `embed-client.ts:272,313`; `embeddings/src/index.ts:213-217`; recall.test.ts:401 |
| AC-7 | Gates green; no secret in IPC/logs/recall; artifact NOT ballooned | **PASS** | gate exit codes below; pack 0 model blobs; bundle 0 token refs |

---

## Five-axis evaluation

**Completeness** — All five coupled scope pieces shipped: (1) default-on inversion + supervisor + first-run heal; (2) store+capture seams threaded to the real embedder; (3) recall cosine arm + honest degraded; (4) lifecycle robustness (warmup off turn path, bounded crash-restart); (5) dim invariant end-to-end. Nothing left as a stub.

**Correctness** — The inversion is real: `enabled = !(raw === "false" || raw === "0")` (`embed-client.ts:172`). The supervisor genuinely spawns/health-checks/crash-restarts (`embed-supervisor.ts:308-359`), warms OFF the turn path (line 341 `warmWatch` never awaited on start), and the embed daemon is a real transformers.js pipeline, not a stub (`embeddings/src/index.ts:187-193`). `degraded = semanticHits === null` (`recall.ts:439`) is honest in both directions.

**Alignment** — D-1..D-6 honored. D-5 (reuse, don't fork) is respected: recall calls the EXISTING `vectorSearch` (`recall.ts:327`), no new vector SQL. D-2 (first-run over bundling) holds: `@huggingface/transformers` is `optionalDependencies` only, model acquired lazily, 0 model blobs packed. D-6 ownership wired into the daemon lifecycle (`assemble.ts:580`).

**Gaps** — None blocking. One nuance documented below (AC-6 live path).

**Detrimental patterns** — None. No `expect(true)`, no production path mocked away as a "test". The recall unit tests are differential (a fake `EmbedClient` drives valid-vector→semantic-ran and null/throw/wrong-dim→lexical, asserting `<#>` issuance via SQL inspection). The AC-4 itest is a true A/B: semantic surfaces the turn + `degraded:false`, lexical-only misses it + `degraded:true`.

---

## Gate exit codes

| Gate | Exit | Notes |
|------|------|-------|
| `npm run ci` | **0** | 1837 passed, 5 skipped; `audit:sql` inline OK (163 files). No flake this run — did NOT hit the known `exec.test.ts`/`api.test.ts` pair. |
| `npm run build` | **0** | daemon + dashboard-web + 5 hook-harness + OpenClaw + MCP + 4 SDK + CLI + embed-daemon bundle @ 0.1.0 |
| `npm run audit:sql` | **0** | every SQL interpolation routes through an escaping helper |
| `npm run audit:openclaw` | **0** | bundle clean against ClawHub static-analysis rules |
| `npm run pack:check` | **0** | 48 files, no forbidden patterns, all required runtime files present |
| `pack dry-run model-blob scan` | **0 matches** | `transformers|onnx|.bin|embed-models|model_quantized` → 0 packed; `embed-daemon.js` + `ensure-embed-deps.mjs` present |
| `tests/daemon/storage/invariant.test.ts` | **0** | 3 passed (embeddings stays DeepLake-free) |
| gated itest skip (token-less) | **0** | 3 skipped, not failed — gate is honest |

**Flake handling:** the known pre-existing flaky pair (`exec.test.ts` "b-AC-5", `sources/api.test.ts`) did NOT fire in this `npm run ci` run (clean on first pass, no re-run needed). Not a PRD-025 regression either way.

---

## AC-7 secret scan (embed IPC / supervisor / model-download / recall)

- `embed-supervisor.ts:165-183` — `scrubChildEnv` STRIPS `HONEYCOMB_DEEPLAKE_TOKEN`/`_ORG`/`_WORKSPACE`/`_ENDPOINT`/`HONEYCOMB_TOKEN` from the embed child's env before spawn. This is the security-bee's remediated High (embed child no longer inherits the token). The only grep hit for token names is this scrub list (the var NAMES being deleted), never a value.
- `embeddings/src/index.ts:234-245` — `redactWarmError()` strips `hf_…` tokens / `Authorization: Bearer` / `token=`/`api_key=` from any surfaced warmup-failure reason on `/health` + stderr.
- `/embed` 503-cold and error bodies carry a generic reason, never the input text or a token (`embeddings/src/index.ts:348,357-359`).
- Built `embeddings/embed-daemon.js`: **0** token references.

---

## Notes (non-blocking)

1. **AC-6 live path is asserted via its invariant, not by driving a wrong-dim vector through the daemon live.** The gated itest (`semantic-recall-live.itest.ts:289-316`) seeds a `content_embedding = NULL` row by direct INSERT and asserts it stays lexically recallable + reads back NULL — i.e. it asserts the state the dim-reject *produces*. The actual wrong-dim → NULL reject is exercised at the unit level in three places (`embed-client.ts:272` client guard, `:313` attacher guard, `embeddings/src/index.ts:213-217` daemon guard) and the recall-side defense-in-depth (`recall.test.ts:401`). The comment at the itest acknowledges this explicitly. Coverage is complete across the write boundary; this is a deliberate, documented test design, not a gap. No action required.

2. **Live behavioral proofs (AC-2/4/5/6 live + AC-1 live) are attested by the orchestrator's Wave-3 run** (`library/ledger/EXECUTION_LEDGER-prd-025.md:124-136`, "gated itest result: 3/3 passed in 17.2s" against the real embed model + live DeepLake, plus the assembled-daemon kill→degrade→restart toggle at 0.58s). This auditor cannot independently re-run the live path (no DeepLake token in the audit environment), but confirmed the gated itest **skips cleanly** (3 skipped, exit 0) when the token is absent — the gate is honest, and the code paths the live run exercises are all independently graded above. The two Wave-3 live-fixes (port-NaN-on-unset-env; `v1.5`→commit-SHA revision + surfaced `warmError`) are present in the code and carry regression tests.

---

## Traceability

| PRD item | Implementation | Test | Status |
|----------|----------------|------|--------|
| D-1 opt-OUT inversion | `embed-client.ts:169-177` | `embed-client.test.ts:186-218` | ✅ |
| D-2 first-run not bundled | `package.json` optionalDeps; `ensure-embed-deps.mjs` | `pack:check` + dry-run scan | ✅ |
| D-3 background warm | `embed-supervisor.ts:341`; `embeddings/src/index.ts:310` | `embed-supervisor.test.ts` (warm off start path) | ✅ |
| D-4 unavailable→clean degrade | `recall.ts:376-392`; supervisor never-live (`:331-337`) | `recall.test.ts:359-417` | ✅ |
| D-5 reuse vectorSearch | `recall.ts:327` (no forked SQL) | `recall.test.ts:319-340` (`<#>` issued) | ✅ |
| D-6 daemon owns embed proc | `assemble.ts:580`; `embed-supervisor.ts` | `assemble.test.ts` (embed is daemon service) | ✅ |
| AC-3 line-255 hard-code removed | `recall.ts:439` (`degraded = semanticHits === null`) | `recall.test.ts:319-431` | ✅ |
| AC-2 store/capture real seam | `assemble.ts:411,465,611` | itest AC-2 + live read-back | ✅ |
