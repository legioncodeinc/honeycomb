# QA Report: PRD-026 — Pollinating loop enablement + live validation

**Plan document:** `library/requirements/in-work/prd-026-pollinating-loop-enablement/prd-026-pollinating-loop-enablement-index.md`
**Audit date:** 2026-06-22
**Base branch:** `main`
**Head:** `main` @ `2eb0349` (PRD-026 implemented in an earlier wave; this is the closing independent verification)
**Auditor:** quality-worker-bee
**Ordering:** ✅ Runs AFTER `security-worker-bee` (CLEAN, 0 findings — `reports/2026-06-22-security-report.md`). Correct order; no re-run triggered.

## Summary

**PASS.** All 6 acceptance criteria are met by real, per-AC, behavior-asserting tests against the live code path — not stubs, tautologies, or mocked-to-pass shims. AC-1's enablement gate is proven through the **real** config-driven trigger (`resolvePollinatingConfig()`, no injected fake decision); AC-2/3/4/5 are proven by two gated live itests that drive the **real** trigger and the **real** daemon worker (`worker.runOnce()`) against live DeepLake + the real Anthropic `memory_pollinating` model, with poll-convergent read-backs and an explicit empty-pass regression guard; AC-6's safety routing is enforced in source (destructive ops map outside the 008c direct-apply allow-list) and re-checked live. The shipped default stays false-safe (D-1): `enabled` defaults `false` in `config.ts:63`, ON is the documented `HONEYCOMB_POLLINATING_ENABLED` knob. Local gates re-confirmed green this session (typecheck, 81 pollinating unit tests, `audit:sql`, `audit:openclaw`). No Critical or Warning findings. Two minor Suggestions only. **Cleared to move to `completed`.**

## Scorecard

| Category      | Status | Notes |
|---------------|--------|-------|
| Completeness  | ✅ | All 6 ACs implemented and covered by dedicated, real tests. |
| Correctness   | ✅ | Live itests assert behavior (subtract-not-zero reset, single-pending guard, consolidation + zero source loss), not shape. |
| Alignment     | ✅ | Matches every Decision: D-1 false-safe default, D-3 008c risk-routed apply, D-5 real `pollinating` model seam, D-6 lineage-auditable. |
| Gaps          | ✅ | No implied-but-missing gaps: fail-soft ack, no-secret ack, fail-closed edge, infra-degraded preflight all present. |
| Detrimental   | ✅ | No tautologies, no mocked-to-pass; empty/garbled-model pass FAILS loudly; itests gated + isolated + DROP-cleaned. |

## Critical Issues (must fix)

None.

## Warnings (should fix)

None.

## Suggestions (consider improving)

- [ ] **AC-2 "exactly ONE enqueue" is proven on a fresh-scope crossing, not on a redundant same-tick double-fire** — `tests/integration/pollinating-counter-live.itest.ts:176-216`

  The single-pending guard test crosses the threshold once (`queue.calls` length 1), then re-ticks while pending and asserts the count stays 1 — which is the correct AC-2 proof. As a hardening nicety, a third assertion that a *second threshold-crossing increment* before the runner clears `pending_job_id` still enqueues nothing would close the "two crossings, one slot" interpretation even more tightly. The current coverage is sufficient; this is belt-and-suspenders.

- [ ] **AC-5 before/after delta is logged but not persisted as a durable artifact** — `tests/integration/pollinating-consolidation-live.itest.ts:407-419`

  The before/after snapshot (the artifact D-1 says "licenses flipping the shipped default") is emitted via `console.log` as `[026 receipt]` lines. It is captured in the itest run log, which satisfies AC-5 as written ("captures … and asserts the delta"). If a later change wants to *cite* the measured delta when flipping the default, consider also writing the receipt to a file under the PRD's `reports/`. Not required by the AC; the assertion-level proof is fully present.

## Plan Item Traceability

| #    | Plan Requirement | Status | Implementation Location | Notes |
|------|------------------|--------|-------------------------|-------|
| AC-1 | Enable flips the live trigger: `enabled:false`→`{triggered:false,status:"skipped",reason:"disabled"}`; `enabled:true`→not-skipped (`enqueued`/`running`), via a real config-driven trigger seam | ✅ | `tests/daemon/runtime/pollinating/api.test.ts:189-249`; gate `src/.../pollinating/config.ts:63,134`; ack mapping `src/.../pollinating/api.ts:166-179,239-258` | The AC-1 block drives the REAL trigger built from `resolvePollinatingConfig()` (no fake decision) against env-toggled `HONEYCOMB_POLLINATING_ENABLED`; env saved+restored per test. Real seam, not a stub. |
| AC-2 | Cadence + single-pending guard hold live: crossing `tokenThreshold` enqueues exactly ONE job, reset SUBTRACTS, a 2nd tick while `pending_job_id` set enqueues nothing; poll-convergent read-back | ✅ | `tests/integration/pollinating-counter-live.itest.ts:135-216`; logic `src/.../pollinating/trigger.ts:365-419` | Live itest: 180−100=80 carried forward (not zeroed, interleaved write not lost); guard holds count at 1 across a live round-trip; reads via `readState` poll-convergence. Gated on `HONEYCOMB_DEEPLAKE_TOKEN`. |
| AC-3 | A real pass consolidates a seeded set: dups merged (or pending merge proposal), stale superseded + newer active, junk archived (or pending) — graph measurably sharper | ✅ | `tests/integration/pollinating-consolidation-live.itest.ts:324-438`; worker `src/.../pollinating/worker.ts`; mapping `src/.../pollinating/contracts.ts:144-152` | Real `worker.runOnce()` end-to-end, real Anthropic model. Accepts "merged OR pending merge proposal" (model honesty). Empty/garbled pass FAILS with explicit message (`:430-433`) — no silent green. |
| AC-4 | Nothing source-backed is lost: a source-backed claim active+provenance-intact before is still active after; before/after survivor count non-decreasing | ✅ | `tests/integration/pollinating-consolidation-live.itest.ts:306-310,396,442-448` | Seeds a survivor claim with `memory_id`; asserts `sourceBackedSurvivors` non-decreasing AND ≥1 active-with-provenance after the pass. Supersede is append-only (`trigger.ts` / D-6). |
| AC-5 | Before/after measurement recorded: dup-count / active-vs-superseded / junk-count snapshot, delta in consolidating direction | ✅ | `tests/integration/pollinating-consolidation-live.itest.ts:107-119,295-311,350-419` | `Snapshot` captured before+after, delta logged as `[026 receipt]`, consolidating direction asserted via `consolidated` poll + the anyConsolidation guard. See Suggestion on persisting the receipt. |
| AC-6 | Safety + gates green: destructive ops → pending review; ack carries no secret; live itests creds-gated (skipped in CI); `npm run ci`/`build`/`audit:sql`/`audit:openclaw`/invariant pass | ✅ | Routing `src/.../pollinating/contracts.ts:144-152`; live safety check `pollinating-consolidation-live.itest.ts:455-460`; no-secret `api.test.ts:150-162,239-248`; gating `vitest.config.ts` + `*.itest.ts` + `describe.skipIf` | Destructive kinds map OUTSIDE 008c `DIRECT_APPLY_OPERATIONS` → always pending; itest re-asserts a dup reduction must be backed by a pending merge proposal. `audit:sql` (197 files OK) + `audit:openclaw` (clean) re-run green this session; typecheck + 81 pollinating unit tests green. |
| D-1  | Shipped default stays OFF; ON is the documented `HONEYCOMB_POLLINATING_ENABLED` knob (trivially flippable, not flipped-on) | ✅ | `src/.../pollinating/config.ts:38-41,63,114-125` | `enabled: BoolFlag.default(false)`; env seam `HONEYCOMB_POLLINATING_ENABLED` (`true`/`1`→on). False-safe confirmed; AC-1 `enabled:false` test proves the default ack is `disabled`. |
| D-3  | Mutations stay on the 008c risk-routed `submitProposal` path; supersede appends, never destroys | ✅ | `src/.../pollinating/contracts.ts:135-152`; worker delegates to runner `submitProposal` (`worker.ts:30-42`) | Worker issues NO direct SQL; all writes via runner `submitProposal` + append-only counter. |
| D-5  | Live pass calls the REAL `pollinating`/`memory_pollinating` ModelClient seam (not a fake) | ✅ | `pollinating-consolidation-live.itest.ts:362-369` | `buildInferenceModelClient(... AGENT_YAML)` → real Anthropic `memory_pollinating`; production resolution path (secrets store + `${ANTHROPIC_API_KEY}`). |

## Files Changed

(PRD-026 was implemented in an earlier wave; this audit verifies the resident surface rather than a fresh diff. Key files traced:)

- `src/daemon/runtime/pollinating/api.ts` (resident) — `POST /api/diagnostics/pollinate` trigger seam; builds the REAL config-driven trigger; fail-soft + no-secret ack.
- `src/daemon/runtime/pollinating/config.ts` (resident) — false-safe `enabled` default (D-1); `HONEYCOMB_POLLINATING_ENABLED` env seam; zod-at-boundary resolve.
- `src/daemon/runtime/pollinating/contracts.ts` (resident) — seven-op mutation vocabulary; destructive→pending mapping outside 008c direct-apply (AC-6/D-3).
- `src/daemon/runtime/pollinating/trigger.ts` (resident) — append-only counter; subtract-not-zero reset; single-pending guard (AC-2).
- `src/daemon/runtime/pollinating/worker.ts` (resident) — daemon-resident pollinating-job consumer; kind-filtered lease `["pollinating"]`; delegates apply to the runner.
- `src/daemon/runtime/assemble.ts` (resident) — `buildGatedPollinatingWorker` (enablement-gated wiring) + `mountPollinateApi` registration.
- `tests/daemon/runtime/pollinating/api.test.ts` (resident) — AC-1 enablement block via the real trigger; no-secret ack; 13 tests green.
- `tests/integration/pollinating-counter-live.itest.ts` (resident) — AC-2 live counter/guard proof; gated + DROP-isolated.
- `tests/integration/pollinating-consolidation-live.itest.ts` (resident) — AC-3/AC-4/AC-5 live consolidation proof; double-gated (token+key); empty-pass guard.
