# Execution Ledger — PRD-059: Project Onboarding and the Projects Page

> Source of truth for the `/the-smoker` run. Survives context loss.
> Branch: `docs/prd-059-projects-onboarding` (docs PR [#124](https://github.com/legioncodeinc/honeycomb/pull/124)); implementation lands on a fresh `feat/` branch.
> Status legend: OPEN · IN PROGRESS · DONE · VERIFIED · BLOCKED

## Routing reality (read first)

- The **beekeeper-suit roster is TypeScript/Node-only** — there is **no React/UI Bee**. All daemon/hooks TS is roster-ownable by `typescript-node-worker-bee`. All dashboard React UI is **BLOCKED** on a routing decision (authorize `react-worker-bee`/`ux-ui-worker-bee` as substitutes, or register a UI Bee).
- Several sub-PRDs carry **unresolved design forks** with only "Lean:" defaults (gate granularity, folder-picker mechanism, switcher persist-vs-relabel). Implementation will encode the lean unless overridden.
- **Parallelism is bounded by shared files:** the daemon route surface (`server.ts`, `projects/scope-enumeration-api.ts`) and dashboard scope (`scope-context.tsx`) are touched by nearly every item. Concurrent bees in one tree corrupt each other; true parallel needs worktree isolation + integration.

## Ledger

| ID | Source | Criterion (abbrev) | Layer | Owner | Status |
|---|---|---|---|---|---|
| M-AC-1 | 059 | Zero bound projects → no row written, "bind to start" prompt | daemon+hooks | typescript-node-worker-bee | VERIFIED (2026-06-26) — capture gate + session-start notice (`capture-handler.ts` `firstRunGateClosed`, `session-start.ts` `BIND_PROJECT_NOTICE`) |
| M-AC-2 | 059 | Zero-state → dashboard shows "Pick a folder to start" CTA | dashboard UI | react-worker-bee | VERIFIED (2026-06-26) — zero bound projects → `FirstRunBindCTA` is the primary Dashboard content (`needs-project.tsx`, gated in `app.tsx` `Outlet`; `first-run-cta.test.tsx`) |
| M-AC-3 | 059 | Pick folder → absolute path bound, capture begins, project appears | daemon + UI | split | VERIFIED (2026-06-26) — daemon `POST projects/bind` writes the absolute path + gate opens; UI: `FolderPicker` posts the chosen path → on bind routes to Projects where it appears (`folder-picker.tsx`, `needs-project.tsx`; `folder-picker.test.tsx`/`first-run-cta.test.tsx`) |
| M-AC-4 | 059 | Projects page lists sourced projects + Add a project | dashboard UI | react-worker-bee | VERIFIED (2026-06-26) — `ProjectsPage` lists locally-bound projects + a top-right "+ Add" menu (`pages/projects.tsx`, route in `registry.tsx`; `projects-page.test.tsx`) |
| M-AC-5 | 059 | Import existing registry project → this device binds same project_id | daemon + UI | split | VERIFIED (2026-06-26) — daemon `POST projects/bind-existing`; UI: the import modal lists `?unbound=1` projects + binds the chosen folder via `bindExistingProject` (`pages/projects.tsx`; `projects-page.test.tsx`) |
| M-AC-6 | 059 | Switcher persists a real scope change or is labeled view-filter | daemon + UI | split | VERIFIED (2026-06-26) — daemon `POST scope/{org,workspace}-switch`; UI: `selectOrg`/`selectWorkspace` now POST the persist routes, project dropdown relabeled "view filter" + feedback (`scope-context.tsx`; `scope-switch-persist.test.tsx`) |
| a-AC-1 | 059a | Zero projects → no `sessions`/`memory`/`memory_jobs` row, no job | hooks/daemon | typescript-node-worker-bee | VERIFIED (2026-06-26) — `capture-handler.ts` no-ops on closed gate (`capture-first-run-gate.test.ts`) |
| a-AC-2 | 059a | Suppressed → one "bind a project" notice per session | hooks | typescript-node-worker-bee | VERIFIED (2026-06-26) — session-start seam notice (`session-start.ts`, `onboarding-notice.test.ts`) |
| a-AC-3 | 059a | Gate check resolves from local store, no DeepLake call | hooks | typescript-node-worker-bee | VERIFIED (2026-06-26) — `hasBoundProjectOnDisk` pure local read (`project-resolver.ts`) |
| a-AC-4 | 059a | After first bind → capture proceeds and persists | hooks/daemon | typescript-node-worker-bee | VERIFIED (2026-06-26) — gate opens with ≥1 binding (`capture-first-run-gate.test.ts`) |
| a-AC-5 | 059a | ≥1 project → unbound folder still hits `__unsorted__` inbox | hooks | typescript-node-worker-bee | VERIFIED (2026-06-26) — inbox fallback resumes (`capture-first-run-gate.test.ts`) |
| b-AC-1 | 059b | Zero projects → CTA is primary dashboard content | dashboard UI | react-worker-bee | VERIFIED (2026-06-26) — `FirstRunBindCTA` ("No active projects? Pick a folder to start") is the primary Dashboard content when zero bound projects, gated on `projectsHydrated` so it never flashes (`needs-project.tsx`, `app.tsx`; `first-run-cta.test.tsx`) |
| b-AC-2 | 059b | Picker enumerated by daemon (loopback), yields absolute path | daemon | typescript-node-worker-bee | VERIFIED (2026-06-26) — `GET fs/browse` (`onboarding-api.ts`, `onboarding-api.test.ts`) |
| b-AC-3 | 059b | Git folder → name pre-filled from canonical remote | daemon | typescript-node-worker-bee | VERIFIED (2026-06-26) — `suggestProjectId` + git marker on browse (`onboarding-api.ts`) |
| b-AC-4 | 059b | Confirm → bind written, gate opens, advances to Projects page | daemon + UI | split | VERIFIED (2026-06-26) — daemon `POST projects/bind` writes the absolute path + opens the gate; UI: `FolderPicker` confirm calls `bindProject` then `onBound` routes to `/projects` (`folder-picker.tsx`, `needs-project.tsx`; `first-run-cta.test.tsx`) |
| b-AC-5 | 059b | Daemon down/local-mode off → plain message + CLI fallback | daemon + UI | split | VERIFIED (2026-06-26) — daemon routes self-gate to local mode (404 in team) → empty browse; UI: the empty/unavailable browse renders a plain message + `honeycomb project bind` hint, never a hang (`folder-picker.tsx`; `folder-picker.test.tsx`) |
| c-AC-1 | 059c | Projects page lists projects + state (paths, remote, counts) | dashboard UI + daemon | split | VERIFIED (2026-06-26) — STATE now SERVED by `GET scope/projects`. Each `ScopeProject` entry now carries: `boundPaths: string[]` (this device's local bindings, network-free), `remote: string` (canonical `remote_signal`, network-free), `memoryCount: number` + `sessionCount: number` (fail-soft grouped aggregates), `lastCapture: string \| null` (max creation ts). Daemon: `scope-enumeration-api.ts` enrichment + `project-counts.ts` (`readProjectCounts`) + catalog `buildMemoryCountsByProjectSql`/`buildSessionCountsByProjectSql`; counts fail-soft to 0/null on a storage error (paths/remote always served). UI wave: render the 5 new fields, replacing the "—" placeholders (`scope-enumeration-api.ts`, `project-counts.ts`; `scope-enumeration-api.test.ts`, `project-counts.test.ts`). UI NOW WIRED: `ScopeProjectSchema` extended with `boundPaths`/`remote`/`memoryCount`/`sessionCount`/`lastCapture` (all `.catch()`-defaulted, fail-soft for an older daemon); `pages/projects.tsx` renders bound path(s), git remote, memory/session counts (`<mem> / <sess>`), and a humanized last-capture (`null`/none → "never"), replacing every `NotYet`/"—" placeholder; the new `projects-page.test.tsx` cases assert the real values render (`src/dashboard/web/wire.ts`, `src/dashboard/web/pages/projects.tsx`; `tests/dashboard/web/projects-page.test.tsx`) |
| c-AC-2 | 059c | `__unsorted__` inbox shown distinctly with size | dashboard UI + daemon | split | VERIFIED (2026-06-26) — inbox SIZE now SERVED: the empty/`''` `project_id` capture bucket is folded onto `__unsorted__` (summed with any explicit `__unsorted__` bucket), so the inbox row's `sessionCount`/`memoryCount` IS the inbox size. UI renders the inbox as its own distinct dashed row using the `__unsorted__` entry's counts in place of "—" (`project-counts.ts` inbox fold; `project-counts.test.ts`, `scope-enumeration-api.test.ts`). UI NOW WIRED: `InboxRow` renders `size {inbox.sessionCount}` (the served inbox-bucket size) via a `data-testid="inbox-size"` cell, replacing the hardcoded "size —"; the new `projects-page.test.tsx` "inbox SIZE" case asserts the real count renders (`src/dashboard/web/pages/projects.tsx`; `tests/dashboard/web/projects-page.test.tsx`) |
| c-AC-3 | 059c | Add a project (top-right) runs folder-pick→bind | dashboard UI | react-worker-bee | VERIFIED (2026-06-26) — top-right "+ Add" menu → "New folder" reveals the `FolderPicker` pick→bind flow (one "+" with two options, parent lean) (`pages/projects.tsx`; `projects-page.test.tsx`) |
| c-AC-4 | 059c | Unbind → folder binding removed, registry+data untouched | daemon + UI | split | VERIFIED (2026-06-26) — daemon `POST projects/unbind` removes the local binding only; UI: per-row Unbind drives a folder-select → `unbindProject({ path })` (path-keyed, the daemon contract) then re-lists (`pages/projects.tsx`; `projects-page.test.tsx`) |
| c-AC-5 | 059c | Open project → other surfaces re-scope | dashboard UI | react-worker-bee | VERIFIED (2026-06-26) — per-row Open calls `useScope().setScope({ …, project })`, re-scoping the memories/graph/sync surfaces (049e view scope), persisted viewer-side (`pages/projects.tsx`; `projects-page.test.tsx`) |
| d-AC-1 | 059d | Import lists registry projects without a local binding | daemon + UI | split | VERIFIED (2026-06-26) — daemon `GET scope/projects?unbound=1` + `boundLocally`; UI: the import modal lists exactly the `unbound` set and binds the chosen folder via `bindExistingProject` (`pages/projects.tsx`, wire `scopeProjects({unbound})`; `projects-page.test.tsx`) |
| d-AC-2 | 059d | Select registry project + folder → binds same project_id | daemon | typescript-node-worker-bee | VERIFIED (2026-06-26) — `POST projects/bind-existing` (`onboarding-api.ts`, `onboarding-api.test.ts`) |
| d-AC-3 | 059d | Imported project recall includes other-device memories | daemon | typescript-node-worker-bee | VERIFIED (2026-06-26) — bind-to-existing writes the shared `project_id`; recall scope is the existing 049 path (no new code; binding proven in `onboarding-api.test.ts`) |
| d-AC-4 | 059d | Git-remote match surfaced as suggestion (hint only) | daemon | typescript-node-worker-bee | VERIFIED (2026-06-26) — browse marks git repos + `suggestProjectId` derives from canonical remote (`onboarding-api.ts`) |
| 122-AC-1 | IRD-122 | Org/workspace switch persists via daemon, or labeled view-only | daemon + UI | split | VERIFIED (2026-06-26) — daemon `POST scope/{org,workspace}-switch` persists; UI: `selectOrg`/`selectWorkspace` POST those persist routes (org change re-mints), so a switch saves to credentials.json instead of being localStorage-only (`scope-context.tsx`; `scope-switch-persist.test.tsx`) |
| 122-AC-2 | IRD-122 | Org change persists → re-mints org-bound token | daemon | typescript-node-worker-bee | VERIFIED (2026-06-26) — `org-switch` re-mints + `saveDiskCredentials` (`scope-switch-api.ts`, `scope-switch-api.test.ts`) |
| 122-AC-3 | IRD-122 | Project dropdown clearly a view filter | dashboard UI | react-worker-bee | VERIFIED (2026-06-26) — the project control is labeled "project · view filter" with a caption ("Filters what you VIEW. To set where Honeycomb captures, bind a folder in Projects.") (`scope-context.tsx`; `scope-switch-persist.test.tsx`) |
| 122-AC-4 | IRD-122 | No switcher change is a silent no-op | daemon + UI | split | VERIFIED (2026-06-26) — every switch surfaces `switchFeedback`: a persisted org/workspace (✓, with the org re-mint loading state), a project view-filter, or a failure (could not switch). No selection is a silent no-op (`scope-context.tsx`; `scope-switch-persist.test.tsx`) |
| 123-AC-1 | IRD-123 | Zero projects → no capture rows/jobs (== a-AC-1) | hooks/daemon | typescript-node-worker-bee | VERIFIED (2026-06-26) — (== a-AC-1) `capture-first-run-gate.test.ts` |
| 123-AC-2 | IRD-123 | One "bind a project" notice per session (== a-AC-2) | hooks | typescript-node-worker-bee | VERIFIED (2026-06-26) — (== a-AC-2) `onboarding-notice.test.ts` |
| 123-AC-3 | IRD-123 | Gate from local store, no network (== a-AC-3) | hooks | typescript-node-worker-bee | VERIFIED (2026-06-26) — (== a-AC-3) `hasBoundProjectOnDisk` |
| 123-AC-4 | IRD-123 | After first bind → capture proceeds (== a-AC-4) | hooks/daemon | typescript-node-worker-bee | VERIFIED (2026-06-26) — (== a-AC-4) `capture-first-run-gate.test.ts` |
| 123-AC-5 | IRD-123 | ≥1 project → inbox fallback resumes (== a-AC-5) | hooks | typescript-node-worker-bee | VERIFIED (2026-06-26) — (== a-AC-5) `capture-first-run-gate.test.ts` |

**Totals:** 34 ACs · 13 cleanly roster-ownable (TS/Node) · 8 BLOCKED (pure UI, no Bee) · 13 split (daemon part ownable now, UI part blocked).

**Close-out status (2026-06-26):** ALL 34 ACs **VERIFIED** by `quality-worker-bee`. 0 PARTIAL · 0 FAIL · 0 merge blockers. See the QA report: [`library/requirements/backlog/prd-059-projects-onboarding/qa/prd-059-projects-onboarding-qa.md`](../requirements/backlog/prd-059-projects-onboarding/qa/prd-059-projects-onboarding-qa.md).

## Wave 1 — independent verification (orchestrator, VERIFIED)

All Wave-1 daemon/hooks ACs flipped DONE→**VERIFIED**: typecheck clean, jscpd 0.51% (<7), audit:sql clean, the 29 new Wave-1 tests pass, neighborhood (capture+projects+hooks) 261/274. The only failures are **13 pre-existing timeouts in `tests/hooks/runtime/hook-runtime.test.ts`** — reproduced identically on a clean baseline (all Wave-1 changes stashed → 13 failed / 10 passed), so NOT a Wave-1 regression. They are slow loopback-I/O tests exceeding the default 5s `vitest` timeout on this machine. **Pre-existing-flake finding (not blocking 059):** the hook-runtime suite needs a raised `--test-timeout` or to be de-flaked; file a separate issue.

## Close-out — security + quality (VERIFIED)

- **security-worker-bee:** ran first. Found + fixed one HIGH symlink path-traversal (CWE-22) in `fs/browse` (`onboarding-api.ts` `canonicalize`/`realpathSync` clamp), committed at HEAD `dc1888b`; covered by `onboarding-api.test.ts` ("refuses to traverse outside the root through a symlink/junction planted INSIDE it"). Everything else clean.
- **quality-worker-bee (2026-06-26, HEAD `dc1888b`):** all 34 ACs flipped DONE→**VERIFIED**. Each AC traced to code + a behavior-asserting passing test (no stub / mock-in-prod / TODO). Gates green: `npm run typecheck` (exit 0), `npm run dup` 0.51% (<7), `npm run audit:sql` clean, `npx vitest run tests/daemon/runtime/projects tests/dashboard/web tests/hooks/shared` = 42 files / 390 tests passed. First-run gate confirmed wired ON in production (`assemble.ts:736`). Two non-blocking doc-drift Warnings (stale comments in `pages/projects.tsx`); the `hook-runtime.test.ts` 13-test timeout is the pre-existing infra flake (reproduced on a clean baseline — NOT a 059 defect). Per-project counts are best-effort (fail-soft to 0 on backend flap), tested with fakes. **Verdict: PASS, clear to merge.**

## Wave plan

- **Wave 1 — daemon/hooks foundation (`typescript-node-worker-bee`, Opus-class for deep daemon reasoning).** One bee, coherent slice (avoids self-conflict on the shared route surface): capture gate (059a/IRD-123), daemon `fs/browse` + `projects bind|bind-existing|unbind` routes, org/workspace switch-persist route (IRD-122 backend), zero-projects predicate. DoD: `npm run ci` green (tsc + jscpd + vitest with fakes). Verifies offline — no live DeepLake needed.
- **Wave 2 — dashboard UI. DONE (react-worker-bee authorized as the UI substitute):** empty-state picker + first-run CTA (059b), Projects page + "+ Add" menu (059c), import modal (059d), switcher persist + relabel (IRD-122). DoD met: `npm run typecheck` GREEN, `npm run dup` 0.51% (<7), full `npm run build` produces the dashboard-web bundle, and 4 new Vitest suites (`folder-picker`/`projects-page`/`first-run-cta`/`scope-switch-persist`) pass with faked daemon fetches (no live network). All 284 dashboard tests green. Marked DONE, NOT VERIFIED (awaiting security + quality close-out).
- **Close-out:** `security-worker-bee` → `quality-worker-bee` on whatever lands.

## Parked blockers (specific asks)

1. **No UI Bee.** Ask: authorize `react-worker-bee` + `ux-ui-worker-bee` as substitutes for Wave 2 (overriding the smoker's no-substitute rule for this run), or defer all UI to a separate effort.
2. **Design forks** (implementation encodes the "Lean" unless told otherwise): (a) gate per-device vs per-workspace; (b) folder-picker = daemon-served browse vs paste-path; (c) IRD-122 = wire org/workspace persist now vs relabel-only for v1.
3. **Branch/PR shape.** Docs PR #124 is open + docs-only. Implementation should land on a fresh `feat/` branch (off the docs branch so PRDs are present), not piled onto #124.
