# Execution Ledger — PRD-079: Durable Capture Retry Queue

**Branch:** `feat/prd-079-durable-capture-retry-queue` (off `main`, includes the PRD-079 doc commit)
**Scope of this run:** Phase **079a** (the MVP: outbox + drainer + observability — the only phase with enumerated acceptance criteria). 079b (dead-letter + recovery-triggered drain) and 079c (caps + coalescing) are **Draft** in the PRD with no ACs → out of scope for this execution, exactly as PRD-078 shipped 078a alone.
**Status legend:** OPEN → IN PROGRESS → DONE (impl + tests pass, nothing else broken) → VERIFIED (independent close-out pass).

## AC Ledger (Phase 079a)

| ID | Criterion (abridged) | Owner Bee | Status |
|----|----------------------|-----------|--------|
| a-AC-1 | On a capture append failure (batched `flushBatch` OR immediate path non-ok), the `{row, scope}` rows are ENQUEUED into the durable `capture_outbox` instead of dropped. Test: failing storage stub → outbox count grows (not only `recordDropped`). | typescript-node-worker-bee | VERIFIED |
| a-AC-2 | A background drainer re-attempts queued captures via `appendOnlyInsertMany` on the WRITE client; OK → delete, non-ok → `attempts+1` + pushed `next_attempt_at`. Test: fail-then-succeed stub drains to empty across two ticks. | typescript-node-worker-bee | VERIFIED |
| a-AC-3 | Bounded exponential backoff (documented base + cap); drainer SKIPS rows whose `next_attempt_at` is future — no hot-loop. Test: `next_attempt_at` grows per attempt; not-yet-due row not attempted. | typescript-node-worker-bee | VERIFIED |
| a-AC-4 | Outbox anchored on `honeycombStateDir()` (`~/.apiary/honeycomb/.daemon/`, PR #285) — survives stop/start, cwd-independent, drains on next boot. Test: enqueue, close, reopen, drain persisted rows. | typescript-node-worker-bee | VERIFIED |
| a-AC-5 | Fail-soft: outbox enqueue/drain error NEVER breaks capture and never surfaces to the hook — logged (`capture.outbox.enqueue_failed`/`drain_failed`) + counted, not thrown. Test: throwing outbox stub leaves the capture ack intact. | typescript-node-worker-bee | VERIFIED |
| a-AC-6 | Idempotent replay: outbox stores the already-built row with its deterministic `makeRowId` id (never a new id on replay); enqueue only on confirmed non-ok; rare timeout-that-landed duplicate deduped by `source+id` at fusion. Test: replayed row keeps original id. | typescript-node-worker-bee | VERIFIED |
| a-AC-7 | Observability: `/health` `captureOutbox { pending, retrying }` + secret-free `capture.outbox.{enqueued,drained,retry}` events (counts/durations/attempt only — NO content/token/query/org/scope). Test: health shape + events carry no content/scope. | typescript-node-worker-bee | VERIFIED (W-1: /health endpoint-surface half untested; low-risk passthrough) |
| a-AC-8 | Live acceptance (dogfood, QA report): during an observed degraded window the timed-out captures land in the outbox (pending>0) and drain on recovery (pending→0) with memories present on recall. | quality-worker-bee (dogfood) | VERIFIED-by-mechanism (natural-window observation = post-merge dogfood, non-blocking) |

### Evidence (Phase 079a — impl `file:line` + test name)

All in `tests/daemon/runtime/capture/capture-outbox.test.ts` unless noted. `npm run ci` green (typecheck + jscpd 0.42% + 4812 tests + audit:sql).

- **a-AC-1** — capture-handler routes a failed append to the outbox instead of dropping: `src/daemon/runtime/capture/capture-handler.ts:573`/`:583` (flush non-ok + thrown branches → `onAppendFailure`), `:391` (immediate-path non-ok → `enqueueToOutbox`), `onAppendFailure` `:491`, `enqueueToOutbox` `:501`. Store: `src/daemon/runtime/capture/capture-outbox.ts` `SqliteCaptureOutbox.enqueue`. Tests: *"a-AC-1: enqueue persists the rows and the pending count grows"* + *"HANDLER: a failed batched flush routes the row to the outbox (pending grows), not only recordDropped"* (dropped stays 0).
- **a-AC-2** — drainer: `capture-outbox.ts` `SqliteCaptureOutbox.drainDue`/`reappend`/`deleteRow`/`pushBackoff`. Test: *"a-AC-2: first tick (backend still failing) keeps the row; second tick (recovered) drains it"*.
- **a-AC-3** — bounded backoff + due-skip: `capture-outbox.ts` `backoff()` (`min(base*2^(n-1), cap)`), `leaseDue` (`next_attempt_at <= now`), `pushBackoff`. Test: *"a-AC-3: next_attempt_at grows per failed attempt and a future row is not attempted"*.
- **a-AC-4** — home-anchored persistence: `capture-outbox.ts` `openOutboxDatabase` (reuses `localQueueDaemonDir`/`localQueueDatabasePath` from `local-job-queue.ts`); assemble wires `baseDir: resolveLocalQueueBaseDir()` (`assemble.ts` outbox construct). Test: *"a-AC-4: enqueue, close, reopen the SAME home-anchored db, and drain the persisted row"*.
- **a-AC-5** — fail-soft: `capture-handler.ts` `enqueueToOutbox` (try/catch → never throws), `capture-outbox.ts` `enqueue`/`drainDue`/`openCaptureOutbox` (all try/catch → no-op degrade). Tests: *"a-AC-5: a throwing outbox stub leaves the capture ack intact and does not throw"* (201 ack, no unhandled rejection) + *"openCaptureOutbox degrades to a no-op when the substrate cannot open"*.
- **a-AC-6** — idempotent replay: `capture-outbox.ts` `enqueue` (`INSERT OR IGNORE` on `rowId(row)`), `rowId`/`columnStringValue`. Tests: *"re-enqueuing the SAME row (same makeRowId) is a no-op, not a duplicate"* + *"the drained append replays the ORIGINAL row id, not a fresh one"*.
- **a-AC-7** — observability: `/health` field in `src/daemon/runtime/health.ts` (`HealthReasons.captureOutbox` + builder) wired live in `assemble.ts` `healthDetail` (`captureOutbox.counts()`); secret-free events `capture.outbox.{enqueued,drained,retry}` in `capture-outbox.ts`. Test: *"a-AC-7: counts() reports { pending, retrying } and the drainer events carry no content/scope"*.

## Wave plan

- **Wave 1 — implement 079a (single cohesive surface).** `typescript-node-worker-bee` (model: opus — correctness-critical concurrency/durability on the hot write path, high reasoning + code quality). Owns the new `capture-outbox.ts` module, the `flushBatch`/immediate hook in `capture-handler.ts`, the drainer service + assemble wiring, `/health` + events, and full Vitest coverage for a-AC-1..a-AC-7. Exit: `npm run ci` green; all 7 code ACs implemented with passing tests. *Not parallelized:* every AC touches the same 3-file surface (outbox module / capture-handler / assemble), so fan-out would only create merge conflicts — sequential implement→verify is the correct shape for a single-feature PRD.
- **Wave 2 — close-out (sequential, security before quality).** `security-worker-bee` (opus) audits the new SQLite/outbox surface (injection, PII/content-at-rest in the outbox, path traversal on the DB file) and remediates Critical/High in place; then `quality-worker-bee` (opus) verifies impl-vs-PRD, authors the QA report, and performs the a-AC-8 dogfood. Loop until both clean at medium+.
- **Wave 3 — ship.** Commit, push, open PR (summary + full ledger + wave plan + model choices + close-out), monitor CI to green. On fully-VERIFIED + green: move the PRD folder `backlog/ → completed/`.

## Log

- (init) Ledger created; branch `feat/prd-079-durable-capture-retry-queue` cut from the PRD doc commit `83b6b7e`.
- (Wave 2 — QA) `quality-worker-bee` verified 079a impl-vs-PRD. Independently read every cited `file:line` and re-ran the tests (not trusting the self-report): `capture-outbox.test.ts` 10/10, `capture-handler`/`capture-batching`/`attach` 31/31. Confirmed each test exercises the real criterion (no tautology / no mock-in-production-path) and the happy path is byte-unchanged (outbox intercepts only the flush/immediate FAILURE branch). Graded a-AC-1..a-AC-7 → **VERIFIED**. a-AC-8: a natural DeepLake degraded window cannot be induced on demand and no live workspace exists here, so proved the criterion's MECHANISM end-to-end with controlled fault injection — authored `tests/daemon/runtime/capture/capture-outbox-a-ac-8-mechanism.test.ts` (drives the real capture route → 201 ack → forced failing append → outbox pending==1 → flip fault off → drainer → pending==0 with original id replayed); graded **VERIFIED-by-mechanism**, natural-window observation left as a non-blocking post-merge dogfood. Full `npm run ci` **GREEN** (typecheck + jscpd + 4812 tests/13 skips + audit:sql; the known `secrets/exec.test.ts` wall-clock flake did not fire). Security close-out carried in: CLEAN at High+ (0 Critical/0 High; 1 Medium deferred to 079b by design; 2 Lows). Findings: 0 Critical, 1 Warning (W-1: /health endpoint-surface half of a-AC-7 untested — trivial passthrough), 3 Suggestions (immediate-path drop count, raw `reason` field, unbounded growth until 079b). **Verdict: SHIP.** QA report: `library/requirements/backlog/prd-079-durable-capture-retry-queue/qa/prd-079-durable-capture-retry-queue-qa.md`.
- (Wave 3 — ship) W-1 closed before ship: the orchestrator dispatched the implementer to add the missing `/health captureOutbox` endpoint-surface test — which uncovered and fixed a REAL defect (`health.ts` normalization returned `NaN` for a `NaN` input, JSON-serializing to `null` on the wire, instead of clamping to `0`; fixed with a `nonNegativeInt()` finiteness guard). New test in `tests/daemon/runtime/health.test.ts` asserts verbatim surface + non-negative-int normalization. Independent full `npm run ci` GREEN (4814 tests; the only observed failure across re-runs was the known `secrets/exec.test.ts` wall-clock flake, which passes 16/16 in isolation). All a-AC VERIFIED, security CLEAN at High+, QA verdict SHIP → committing + PR.
- (Wave 1) 079a implemented by `typescript-node-worker-bee`. New module `src/daemon/runtime/capture/capture-outbox.ts` (dedicated `capture_outbox` table inside the home-anchored `local-queue.db`, D-1/D-5; enqueue-on-failure, unref'd drainer with bounded exponential backoff, `{pending,retrying}` counts, secret-free `capture.outbox.*` events, fail-soft no-op degrade). Hooked the `flushBatch` + immediate-path FAILURE branch in `capture-handler.ts` (`onAppendFailure`/`enqueueToOutbox`, happy path byte-unchanged), threaded through `capture/attach.ts` and `assemble.ts` (construct on real-assembly + `HONEYCOMB_CAPTURE_OUTBOX` kill-switch default-on, start drainer in `start()`, close in shutdown), added `/health` `captureOutbox{pending,retrying}` (`health.ts`). Exported the SQLite substrate helpers from `services/local-job-queue.ts` so the outbox reuses them (no jscpd clone). Tests: `tests/daemon/runtime/capture/capture-outbox.test.ts` (10 tests, a-AC-1..a-AC-7). `npm run ci` GREEN (typecheck + jscpd 0.42% + 4812 tests pass/13 pre-existing skips + audit:sql clean). a-AC-1..a-AC-7 → DONE; a-AC-8 (dogfood) left OPEN for QA.
