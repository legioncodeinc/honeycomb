# Execution Ledger — PRD-079 Phases 079b + 079c

**Branch:** `feat/prd-079bc-outbox-deadletter-scale` (off `main` @ b0713ae, post PR #287)
**Scope:** the two remaining PRD-079 phases, now elaborated from Draft into concrete ACs (Phase 0). 079a shipped in #287.
**PRD:** `library/requirements/in-work/prd-079-durable-capture-retry-queue/` (moved completed/ → in-work/ for the duration; returns to completed/ when 079b+079c ship).
**Status legend:** OPEN → IN PROGRESS → DONE (impl + tests pass, nothing else broken) → VERIFIED (independent close-out pass).

## AC Ledger

### 079b — dead-letter + recovery-triggered drain
| ID | Criterion (abridged) | Owner | Status |
|----|----------------------|-------|--------|
| b-AC-1 | Row hitting `maxAttempts` (def 10) OR `maxAgeMs` (def 24h) → terminal `dead` (retained, not re-leased); env-overridable config. | typescript-node-worker-bee | VERIFIED |
| b-AC-2 | Secret-free `capture.outbox.dead_lettered` event + `/health` `deadLettered` count; `counts()` → `{pending,retrying,deadLettered}` with `dead` excluded from active. | typescript-node-worker-bee | VERIFIED |
| b-AC-3 | Recovery-triggered drain kick on next successful capture append and/or `deeplake.woke`, single-flighted + fail-soft. | typescript-node-worker-bee | VERIFIED |
| b-AC-4 | `honeycomb capture drain` operator command forces a pass + prints drained/retried/deadLettered; read-through fail-soft. | typescript-node-worker-bee | VERIFIED |
| b-AC-5 | Fail-soft + non-regression: dead-letter/kick/CLI never break capture, never leak secrets, 079a happy path unchanged. | typescript-node-worker-bee | VERIFIED |

**079b evidence (impl → `file:line`, test → name).** `npm run ci` = green (typecheck + jscpd 0.65% dup ≪ 7% + `vitest run` 453 files / 4832 passed, 13 skipped, 0 failed + audit:sql OK).

- **b-AC-1** — dead-transition in the failed-reappend branch: `capture-outbox.ts:483` (`else if (this.deadLetter(lease))`) → `deadLetter()` `:529` (checks `attempt >= maxAttempts` OR `ageMs >= maxAgeMs`) → `markDead()` `:627` (`status = dead`, retained); `leaseDue` filters `status = pending` (`:610`) so a dead row is never re-leased. Config: `CaptureOutboxLimits` + `resolveCaptureOutboxLimits()` `capture-outbox.ts:135` (defaults 10 / 24h, env `HONEYCOMB_CAPTURE_OUTBOX_MAX_ATTEMPTS`/`_MAX_AGE_MS`, coerce-and-clamp), threaded at the composition root `assemble.ts:3013`. Tests (`tests/daemon/runtime/capture/capture-outbox.test.ts`): "a row that fails maxAttempts re-appends transitions pending → dead (retained, not re-leased)", "a row older than maxAgeMs dead-letters on its next failed attempt".
- **b-AC-2** — secret-free `capture.outbox.dead_lettered { attempt, ageMs, count }` in `deadLetter()` `capture-outbox.ts:547`; `counts()` single-pass partition (`dead` excluded from pending/retrying) `:346`; `/health` `captureOutbox.deadLettered` `health.ts` (`HealthReasons.captureOutbox` + `buildHealthDetail` `nonNegativeInt(... ?? 0)`), fed live from `counts()` at `assemble.ts:3172`. Tests: "dead-lettering emits a secret-free event and excludes dead from pending/retrying" (capture-outbox.test.ts) + "captureOutbox { pending, retrying, deadLettered } surfaces on /health…" (health.test.ts).
- **b-AC-3** — `kick()` single-flighted through the `draining` guard + fail-soft `capture-outbox.ts:516`; handler `kickOutboxDrain()` `capture-handler.ts:525` called on BOTH success branches — immediate `:397`, `flushBatch` `:600`. `deeplake.woke` arm: the outbox drainer is a hibernation `Pausable` whose `resume()` (on wake) re-arms + kicks — `assemble.ts:4125`. **Both arms of the "and/or" wired** (successful-append is the primary; `deeplake.woke` via the resume-kick is the secondary). Test: "a queued row drains via the recovery kick, without arming/awaiting the 30s interval" (capture-outbox.test.ts).
- **b-AC-4** — daemon route `POST /api/diagnostics/capture-drain` forcing one `drainDue` + returning `{ ok, drained, retried, deadLettered }`: `capture-drain-api.ts:79` (`mountCaptureDrainApi`), mounted at `assemble.ts:3388` (fail-soft, outbox-wired only). CLI `honeycomb capture drain`: `commands/capture.ts:57` (`runCaptureVerb`), verb in `VERB_TABLE` (`contracts.ts`), dispatched in `dispatch.ts`. Tests: "POST /api/diagnostics/capture-drain forces one drain + returns the counts" + "fail-soft: a throwing drainDue degrades to a zero-count 200" (capture-drain-api.test.ts); "capture drain POSTs … and renders the counts", "read-through fail-soft: a daemon-down (send rejects) path reports cleanly and never throws" (commands/capture.test.ts).
- **b-AC-5** — recovery-kick fail-soft `capture-handler.ts:525` (`kickOutboxDrain` try/catch → `capture.outbox.kick_failed`, ack unaffected); dead-transition fail-soft `capture-outbox.ts:536` (`deadLetter` guards `markDead` — a fault leaves the row pending, no pass abort); 079a happy path + accounting unchanged (existing a-AC suite green, only the additive `deadLettered: 0` field added to `counts()`/`drainDue()` shapes). Test: "a throwing recovery kick leaves the capture ack intact and escapes no rejection" (capture-outbox.test.ts).

### 079c — caps + coalescing at scale
| ID | Criterion (abridged) | Owner | Status |
|----|----------------------|-------|--------|
| c-AC-1 | Row-count cap `maxRows` (def 10k): shed oldest-first over the cap, each shed COUNTED via `capture.outbox.shed` — never silent; `dead` excluded from the cap. | typescript-node-worker-bee | VERIFIED |
| c-AC-2 | Coalesced drain: same-scope + same-column-signature rows → one multi-row append; heterogeneous shapes grouped separately (buildInsertMany same-columns); failed group backs off each member. | typescript-node-worker-bee | VERIFIED |
| c-AC-3 | Back-pressure `maxDrainPerInterval` (def 200): a pass attempts at most N rows; remainder left due. | typescript-node-worker-bee | VERIFIED |
| c-AC-4 | Fail-soft + observability preserved across cap/coalesce/back-pressure; `/health` honest under load. | typescript-node-worker-bee | VERIFIED |

**079c evidence (impl → `file:line`, test → name).** `npm run ci` = GREEN (exit 0): typecheck + jscpd 0.65% dup ≪ 7% + `vitest run` 453 files / 4844 passed, 13 skipped, 0 failed + audit:sql OK. NOT committed. All surface in `src/daemon/runtime/capture/capture-outbox.ts`; tests in `tests/daemon/runtime/capture/capture-outbox.test.ts`.

- **c-AC-1** — cap + oldest-first shed: `shedToCap()` `capture-outbox.ts:504` (SELECT COUNT(*) WHERE `status = pending`; if `pending − maxRows > 0`, one targeted `DELETE … WHERE id IN (SELECT id … WHERE status = pending ORDER BY created_at ASC, id ASC LIMIT overflow)`), called from `enqueue()` after the insert `:493` (only when rows were added). Secret-free `capture.outbox.shed { count }` `:523`; fail-soft `capture.outbox.shed_failed` `:526`. `dead` rows excluded by the `status = pending` filter. Config: `maxRows` on `CaptureOutboxLimits` `:135`, default `DEFAULT_OUTBOX_MAX_ROWS = 10_000` `:121`, env `HONEYCOMB_CAPTURE_OUTBOX_MAX_ROWS` (`CAPTURE_OUTBOX_MAX_ROWS_ENV`), resolved at `resolveCaptureOutboxLimits()` `:176`, threaded at the composition root `assemble.ts:3013` (`...resolveCaptureOutboxLimits()`). Tests: "enqueuing past maxRows sheds the OLDEST pending rows, bounds pending at maxRows, logs the shed count", "dead rows do NOT count toward the active cap and are never shed by the cap path".
- **c-AC-2** — coalesced drain: `drainDue()` groups the leased due rows via `groupDue()` `:696` (parses each row, deletes corrupt up front, keys by `groupKey(org, workspace, row)` `:843` = scope + ordered column-name signature, NUL-joined) then re-appends each group with ONE `reappendMany()` `:727` → `appendOnlyInsertMany([...rows])`. Per-row accounting preserved: on group OK, delete EVERY member (`drained += 1` each) `:582`; on group non-ok/throw, fail EACH member independently — same-as-079b `deadLetter(member.lease)` check `:592` then `pushBackoff` + `retry` event `:591`. Heterogeneous shapes land in separate groups so `buildInsertMany`'s same-columns assertion never trips. Tests: "N same-scope/same-shape rows drain in ONE multi-row append", "heterogeneous column shapes (user vs assistant/usage) split into SEPARATE appends", "same shape but DIFFERENT scope splits into separate appends", "a FAILED coalesced group backs off EVERY member independently (no row lost, no hot-loop)", "a failed group dead-letters ONLY the member that hit maxAttempts; its sibling backs off". (FIX-1 079a-hardening test updated to heterogeneous shapes so two groups → two append calls; the throw-doesn't-abort-the-pass guarantee is preserved and strengthened.)
- **c-AC-3** — back-pressure + RECONCILIATION: `maxDrainPerInterval` is now the SINGLE authoritative per-pass attempt cap. `leaseDue()`'s `LIMIT` binds `this.maxDrainPerInterval` `:752` (was the 079a `drainBatch`), so a pass leases/attempts at most N rows and the remainder stays due. The old `drainBatch` option + `DEFAULT_OUTBOX_DRAIN_BATCH` (50) were UNIFIED into this one knob: `DEFAULT_OUTBOX_DRAIN_BATCH` is retained only as a `@deprecated` alias of `DEFAULT_OUTBOX_MAX_DRAIN_PER_INTERVAL` `:115` (no external importer breaks); the `drainBatch` construction option is replaced by `maxDrainPerInterval`. Default `DEFAULT_OUTBOX_MAX_DRAIN_PER_INTERVAL = 200` `:108`, env `HONEYCOMB_CAPTURE_OUTBOX_MAX_DRAIN_PER_INTERVAL`, resolved `:177`, threaded at `assemble.ts:3013`. Test: "a backlog larger than the cap attempts at most maxDrainPerInterval rows in one pass".
- **c-AC-4** — fail-soft + honest observability: shed (`shedToCap` try/catch `:504`), coalesce (`reappendMany` catches a throw → `false` → per-member backoff `:727`; `drainDue` pass-level catch unchanged), and back-pressure (a pure `LIMIT`, plus `clampIntKnob` clamps a fat-fingered knob up to ≥ 1, never throws) all degrade to the pre-079c behavior. `counts()` shape unchanged (`{ pending, retrying, deadLettered }`), so `/health` stays honest; new events are secret-free (only `count` / `attempt` / `ageMs` / `durationMs` / `reason`). Tests: "resolveCaptureOutboxLimits documents defaults + clamps a fat-fingered scale knob (never throws)", "a coalesced-drain fault degrades to the pre-079c per-member backoff and escapes no rejection", "the shed + coalesce + back-pressure events carry NO content / org / workspace (secret-free)", "/health counts stay honest under a bounded, failing backlog (pending never lies)".

## Wave plan

- **Wave 1 — 079b (dead-letter + recovery drain + CLI).** `typescript-node-worker-bee` [opus]. Surface: `capture-outbox.ts` (dead status + maxAttempts/maxAgeMs + counts partition + a `kick()`), `capture-handler.ts` (fire the recovery kick on a successful append), `assemble.ts` (`deeplake.woke` signal wiring + drain-kick thread + CLI seam), `health.ts` (`deadLettered`), a `honeycomb capture drain` CLI command. Exit: `npm run ci` green; b-AC-1..b-AC-5 with tests.
- **Wave 2 — 079c (caps + coalescing + back-pressure).** `typescript-node-worker-bee` [opus], on top of 079b (same file). Surface: `capture-outbox.ts` (cap/shed, column-signature coalescing on drain, `maxDrainPerInterval`). Exit: `npm run ci` green; c-AC-1..c-AC-4 with tests. *Sequential after 079b* — both phases mutate `capture-outbox.ts`, so parallel Bees would collide; 079c also builds on 079b's counts/dead accounting.
- **Wave 3 — close-out (security before quality).** `security-worker-bee` [opus] (shed/CLI/coalesce surface: injection, DoS/shed-abuse, secret-free events, CLI authz) → `quality-worker-bee` [opus] (verify b/c ACs vs PRD, QA report). Loop to clean at medium+.
- **Wave 4 — ship.** Commit, push, PR, CI to green. On fully VERIFIED + green: flip 079b/c phase status → Shipped and move the PRD in-work/ → completed/.

## Log

- (Wave 3 — QA close-out) `quality-worker-bee` [opus] independently verified b-AC-1..b-AC-5 + c-AC-1..c-AC-4 against the PRD (read the cited code + tests, confirmed no tautology / no mock-in-production-path, ran the suites). **All 9 ACs → VERIFIED.** Targeted runs: `tests/daemon/runtime/capture/` + `tests/commands/capture.test.ts` + `tests/daemon/runtime/health.test.ts` = 230/230 pass. The four flagged guarantees each confirmed genuine: (a) c-AC-2 failed-coalesced-group accounting is per-member independent (A dead-letters while sibling B backs off in ONE failed group — no row lost/double-counted); (b) b-AC-1 `maxAgeMs` path dead-letters on age with `maxAttempts` set huge so age is the sole trigger; (c) c-AC-3 `maxDrainPerInterval` is the SINGLE authoritative per-pass lease cap (old `drainBatch` unified to a `@deprecated` alias, no second knob); (d) 079a+079b non-regressed (only the additive `deadLettered` field on `counts()`/`drainDue()`/`/health`; FIX-1 hardening test strengthened). Security folded in: CLEAN at High+ (0 Crit / 0 High, 2 Lows), **route-authz PASS** (drain route on `/api/diagnostics`, `protect:true` per `server.ts:91`). Full `npm run ci` GREEN modulo ONE wall-clock timeout flake (`assemble.test.ts` "PRD-022 … NO org falls back" timed out at 5000ms under parallel load; **passes 42/42 in 320ms in isolation** — same load-flake class the invoker pre-noted for `secrets/exec.test.ts`, unrelated to 079b/c). No production code modified by QA. QA report extended with the "Phase 079b + 079c" section (scorecard, per-AC traceability, findings S-4/S-5 non-blocking, verdict). **Overall: SHIP.** NOT committed.
- (Phase 0) 079b/079c elaborated from Draft into concrete ACs (b-AC-1..5, c-AC-1..4) in the PRD; phase-table statuses → in-work; PRD moved completed/ → in-work/; ledger created; branch `feat/prd-079bc-outbox-deadletter-scale` cut from `main` @ b0713ae.
- (Wave 2) 079c implemented c-AC-1..c-AC-4 → DONE, on top of 079b in the same file `src/daemon/runtime/capture/capture-outbox.ts`. Surface: cap + oldest-first shed (`shedToCap()` called from `enqueue`, `capture.outbox.shed`/`shed_failed`, `maxRows` + `DEFAULT_OUTBOX_MAX_ROWS` 10k + `HONEYCOMB_CAPTURE_OUTBOX_MAX_ROWS`); coalesced drain (`groupDue()` + `groupKey()` scope+column-signature grouping, `reappendMany()` one `appendOnlyInsertMany` per group, per-member independent fail/dead-letter accounting preserved); back-pressure (`maxDrainPerInterval` as the SINGLE per-pass lease cap — RECONCILED with the old 079a `drainBatch`/`DEFAULT_OUTBOX_DRAIN_BATCH` by unifying into one knob, `DEFAULT_OUTBOX_MAX_DRAIN_PER_INTERVAL` 200 + `HONEYCOMB_CAPTURE_OUTBOX_MAX_DRAIN_PER_INTERVAL`, the deprecated alias retained). `CaptureOutboxLimits` gained `maxRows` + `maxDrainPerInterval`; `resolveCaptureOutboxLimits()` resolves them; they thread at the composition root via the existing `...resolveCaptureOutboxLimits()` spread (`assemble.ts:3013`) — no assemble edit needed. Tests: c-AC-1..c-AC-4 blocks added to `tests/daemon/runtime/capture/capture-outbox.test.ts` (11 new cases); the FIX-1 079a-hardening test updated to heterogeneous shapes (coalescing puts two shapes in two groups → two append calls) so its throw-doesn't-abort-the-pass guarantee is preserved. No Deeplake schema change; SQL via `sqlIdent`; timers unref'd (unchanged); secret-free events; no dangling rejections. `npm run ci` GREEN (exit 0): typecheck + jscpd 0.65% + `vitest run` 453 files / 4844 passed, 13 skipped, 0 failed + audit:sql OK. NOT committed.
- (Wave 1) 079b implemented b-AC-1..b-AC-5 → DONE. Surface: `capture-outbox.ts` (`dead` status + `deadLetter()`/`markDead()` + `maxAttempts`/`maxAgeMs` + `resolveCaptureOutboxLimits()` + `counts()` 3-way partition + `kick()`; `CaptureOutboxCounts`/`CaptureOutboxDrainResult` gained an additive `deadLettered`), `capture-handler.ts` (`kickOutboxDrain()` on both append-success branches), `capture-drain-api.ts` (NEW — `POST /api/diagnostics/capture-drain`), `commands/capture.ts` (NEW — `honeycomb capture drain`) + `contracts.ts`/`dispatch.ts`/`index.ts` wiring, `health.ts` (`captureOutbox.deadLettered`), `assemble.ts` (limits resolve + drain-route mount + `deeplake.woke` resume-kick Pausable). Tests added: `tests/daemon/runtime/capture/capture-drain-api.test.ts`, `tests/commands/capture.test.ts`, + b-AC-1..b-AC-5 blocks in `tests/daemon/runtime/capture/capture-outbox.test.ts`; existing 079a `counts()`/`drainDue()` + `/health` assertions updated for the additive `deadLettered` field. `deeplake.woke` seam: a clean callback did not exist (it is a log-only transition), so the drainer was added to the hibernation `Pausable` set — `resume()` (fired on wake, i.e. `deeplake.woke`) re-arms + kicks; the successful-append kick is the primary trigger. `npm run ci` GREEN (exit 0). NOT committed.
